Title: Shapiro polynomials $P_n$
Definition: >-
  The Shapiro polynomials CITE{Wiki}, also called the Rudin-Shapiro
  polynomials, are the pair of polynomial sequences
  $P_n,Q_n\in\mathbb Z[x]$ defined by $P_0=Q_0=1$,
  $P_{n+1}=P_n+x^{2^n}Q_n$, and $Q_{n+1}=P_n-x^{2^n}Q_n$. This table holds
  $P_n$.
Parameters:
  n:
    type: Z
    title: construction index
    display: $n$
    constraints: $n$ is a nonnegative integer
Comments:
  comment-index: >-
    The table is indexed by the construction parameter $n$, not by the degree:
    $P_n$ has degree $2^n-1$.
  comment-variable: >-
    This table writes the polynomial variable as $x$.
Formulas:
  formula-rudin-shapiro-coefficients: >-
    Equivalently, $P_n(x)=\sum_{j=0}^{2^n-1}a_jx^j$, where $a_j=1$ if the
    binary expansion of $j$ has an even number of pairs of consecutive ones,
    counting overlapping pairs separately, and $a_j=-1$ otherwise.
  formula-one-polynomial-recurrence: >-
    The polynomials also satisfy
    $P_{n+1}(x)=P_n(x^2)+xP_n(-x^2)$.
  formula-q-from-p: >-
    $Q_n(x)=(-1)^nx^{2^n-1}P_n(-1/x)$.
  formula-complementary: >-
    If $d=2^n-1$, then
    $P_n(x)x^dP_n(1/x)+Q_n(x)x^dQ_n(1/x)=2^{n+1}x^d$. On the unit circle this
    is $|P_n(z)|^2+|Q_n(z)|^2=2^{n+1}$.
  formula-specialisations: >-
    $P_n(1)=2^{\lfloor(n+1)/2\rfloor}$. Also, $P_n(-1)=0$ for odd $n$ and
    $P_n(-1)=2^{n/2}$ for even $n$.
Programs:
  program-sage:
    language: Sage
    code: |-
      import numberdb.sage as numberdb
      from sage.rings.integer_ring import ZZ
      from sage.rings.polynomial.polynomial_ring_constructor import PolynomialRing

      R = PolynomialRing(ZZ, "x")
      x = R.gen()

      def shapiro_pair(n):
          P = R.one()
          Q = R.one()
          for k in range(n):
              P, Q = P + x**(2**k)*Q, P - x**(2**k)*Q
          return P, Q

      shapiro_pair(8)[0]      # the next polynomial after this table
Similar tables:
  - table: HREF{T318}[Fekete polynomials $f_p$]
    relation: use a quadratic-residue sign rule for their coefficients, rather than the Rudin-Shapiro rule
Links:
  Wiki:
    title: 'Wikipedia: Shapiro polynomials'
    url: https://en.wikipedia.org/wiki/Shapiro_polynomials
Keywords:
  - Rudin-Shapiro polynomials
  - Golay-Rudin-Shapiro sequence
  - Littlewood polynomials
  - binary sequences
Tags:
  - polynomial
  - combinatorics
Data properties:
  type: Z[]
  rigour: exact
  complete: 'no'
  complete-note: >-
    it holds every construction index $n$ with $0\leq n\leq7$, stopping before
    the printed polynomials reach about two thousand characters; $P_8$ has
    1,932 characters
  rigour details: >-
    The generator computes $P_n$ and $Q_n$ by the exact recurrence in
    $\mathbb Z[x]$. Before the draft was filled, every entry was checked
    against the direct Golay-Rudin-Shapiro coefficient construction in
    CITE{formula-rudin-shapiro-coefficients}, against the complementary
    identity in CITE{formula-complementary}, against the recurrence in
    CITE{formula-one-polynomial-recurrence}, against the first three
    polynomials printed by CITE{Wiki}, and against the specialisations in
    CITE{formula-specialisations}.
Display properties:
  number-header: $P_n(x)$
