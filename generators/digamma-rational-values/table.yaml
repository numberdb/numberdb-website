# Draft document for T226, values of the digamma function at rational arguments.
# It began as proposal 1 of agents/table-ideas/BATCH-2026-09-13T0847.md.
# Created through the API with X-Draft: yes, then filled by generate.py beside
# it. Publishing is a person's act.
Title: Values of the digamma function $\psi(x)$ at rational numbers
Definition: >-
  This table gives the digamma function $\psi(x)=\Gamma'(x)/\Gamma(x)$ at
  rational arguments, using the DLMF convention CITE{DLMFPsi}, and the
  harmonic-number continuation $H_x=\psi(x+1)+\gamma$ CITE{WikiHarmonic}.
  Only finite real values are listed.
Keywords:
  - digamma function
  - psi function
  - harmonic numbers of fractional argument
  - generalized harmonic number
  - Gauss digamma theorem
Parameters:
  x:
    type: Q
    title: argument
    display: $x$
    constraints: >-
      $x\in\mathbb{Q}$; for $\psi(x)$, $x$ is not a non-positive integer; for
      $H_x$, $x$ is not a negative integer
  quantity:
    type: Symbolic
    title: quantity
    display: quantity
    values:
      psi: $\psi(x)$
      H: $H_x$
Comments:
  comment-convention: >-
    The symbol $\psi$ means $\Gamma'/\Gamma$, not the shifted function
    sometimes written $\Psi(x)=\psi(x+1)$. In this table $H_x$ is the same
    shift with $\gamma$ added, so $H_n$ is the usual $n$th harmonic number for
    nonnegative integers $n$.
  comment-range: >-
    The listed rational arguments have denominators at most $12$ and lie in
    $-4<x\leq 4$. Integer shifts are listed because
    CITE{formula-recurrence} changes the value by rational summands.
  comment-exact-harmonic: >-
    For a nonnegative integer $n$, the row $H_n$ is the exact rational number
    $\sum_{k=1}^{n}1/k$. The rows $\psi(n)$ still involve $\gamma$ and are
    stored as real values.
Formulas:
  formula-harmonic: >-
    $H_x=\psi(x+1)+\gamma$.
  formula-recurrence: >-
    $\psi(x+1)=\psi(x)+1/x$, and $H_x=H_{x-1}+1/x$ when both sides are finite
    CITE{DLMFFunctional}.
  formula-reflection: >-
    $\psi(1-x)-\psi(x)=\pi\cot(\pi x)$ for nonintegral $x$
    CITE{DLMFFunctional}.
  formula-gauss: >-
    For $1\leq p<q$,
    $\psi(p/q)=-\gamma-\log(2q)-\frac{\pi}{2}\cot(\pi p/q)
    +2\sum_{n=1}^{\lfloor(q-1)/2\rfloor}
    \cos(2\pi pn/q)\log\sin(\pi n/q)$ CITE{WikiDigamma}.
  formula-dirichlet-l: >-
    For a nontrivial Dirichlet character $\chi$ modulo $q$,
    $L(1,\chi)=-q^{-1}\sum_{a=1}^{q}\chi(a)\psi(a/q)$, as in
    HREF{Values_of_Dirichlet_L-functions_at_positive_integers}[the table of
    Dirichlet $L$-values].
Programs:
  program-sage:
    language: Sage
    code: |-
      import numberdb.sage as numberdb
      from sage.rings.rational_field import QQ
      from sage.rings.real_arb import RealBallField

      RBF = RealBallField(numberdb.bits(100, losing=96))
      x = QQ(1) / QQ(3)
      RBF(x).psi()                         # psi(1/3)
      RBF(x + 1).psi() + RBF.euler_constant()  # H_{1/3}
Similar tables:
  - table: HREF{Values_of_the_Gamma_function_at_rational_numbers}[Values of the Gamma function at rational numbers]
    relation: $\psi$ is the logarithmic derivative of $\Gamma$
  - table: HREF{Zeros_of_the_polygamma_functions}[Zeros of the polygamma functions $\psi^{(n)}$]
    relation: stores the real zeros of the same function and its higher derivatives
  - table: HREF{Values_of_the_Hurwitz_zeta_function_at_pairs_of_rational_numbers}[Values of the Hurwitz zeta function at pairs of rational numbers]
    relation: higher polygamma values satisfy $\psi^{(m)}(x)=(-1)^{m+1}m!\zeta(m+1,x)$
  - table: HREF{Values_of_Dirichlet_L-functions_at_positive_integers}[Values of Dirichlet L-functions at positive integers]
    relation: uses digamma values in the formula for $L(1,\chi)$
  - table: HREF{Stieltjes_constants}[Stieltjes constants]
    relation: contains $\gamma_0$, the Euler-Mascheroni constant appearing in $H_x$
Links:
  DLMFPsi:
    title: 'DLMF 5.2: Gamma and Psi Functions'
    url: https://dlmf.nist.gov/5.2
  DLMFFunctional:
    title: 'DLMF 5.5: Functional Relations'
    url: https://dlmf.nist.gov/5.5
  WikiDigamma:
    title: 'Wikipedia: Digamma function'
    url: https://en.wikipedia.org/wiki/Digamma_function
  WikiHarmonic:
    title: 'Wikipedia: Harmonic number'
    url: https://en.wikipedia.org/wiki/Harmonic_number
Tags:
  - special values
  - special functions
Data properties:
  type: R
  rigour: proven
  rigour details: >-
    Each non-exact value is computed as a Sage real ball with arb at
    `numberdb.bits(digits, losing=96)` bits, using exact rational arguments.
    The entries were checked against Gauss's digamma theorem
    CITE{formula-gauss}, the recurrence in CITE{formula-recurrence}, the
    reflection formula in CITE{formula-reflection}, the defining relation for
    $H_x$ in CITE{formula-harmonic}, the exact rational values of $H_n$ for
    nonnegative integers $n$, and the Dirichlet $L$-value relation in
    CITE{formula-dirichlet-l}.
  complete: no
  complete-note: >-
    it holds both quantities for every rational argument $x=a/b$ in lowest
    terms with $b\leq12$ and $-4<x\leq4$ at which the quantity is finite
Display properties:
  number-header: value
