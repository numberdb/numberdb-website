* *done* -- Expanded the numbering comment to state the Perko-pair shift and SnapPy's $10_{83}$/$10_{86}$ interchange; checked the affected SnapPy names with `agents/sage.sh /tmp/t316_verify_numbering.py` against the live stored values.
* *done* -- Cited `AdamsHildebrandWeeks` in the cusp-convention comment; checked the live document first and confirmed the reference had no `CITE{AdamsHildebrandWeeks}` use.
* *done* -- Made the Definition identify $\operatorname{cv}(S^3\setminus K)$ as the volume of the largest embedded cusp neighbourhood, then shortened it after audit; checked the final audit is clean.
* *done* -- Changed the Chern-Simons Similar-tables caption to the live T313 title; checked T313 through the API before changing only the caption text.
* *done* -- Changed the SnapPy link URL to the `cusp_areas` method anchor; checked that URL returns 200.
* *done* -- Added `# sage -pip install snappy` to the `Programs` import line; checked the live program block still imported `snappy`.
* *done* -- Narrowed the relation repair to the Jones table, adding that chiral knots are listed with their mirrors there; checked T139 has 479 entries and T138's live Alexander table does not split chiral knots, so the Alexander relation was left unchanged.
