1. *done* -- Replaced the Wikipedia convention sentence with the explicit relation $p_n(z)=W_n(-z;a,b,c,d)$ here; checked in Sage that the $a\pm t$ product gives $W_n(-y)$ in two nontrivial cases and disagrees without the sign change.
2. *done* -- Removed the workflow sentence naming `agents/table-build/dry_run.py` from `rigour details`; checked the generator integrity run, dry-run exactness and size measurement, and stored-value verification before changing only the prose.
3. *done* -- Replaced the repeated symmetry clause in `complete-note` with the reason for the grid: a compact set of five small integral and half-integral shape values staying below about half the soft entries-block limit; checked the dry run reports 770 exact entries, longest 450 characters, block 141.3 KB.
4. *done* -- Removed the uncited `DLMFConstraints` link rather than citing it against a statement it does not exactly support; checked the rendered preview, stored document, and audit after the edit.
5. *declined* -- Left the definition's rational ordered parameters alone because the critique marked the possible rewrite as not worth a change on its own, and the local proposal says the order is the table's representative for the symmetry.
6. *declined* -- Left the title and value variable convention alone because the definition and `comment-variable` define $y=x^2$, and storing Wilson polynomials as degree-$n$ polynomials in $y$ is the table's intended convention.
7. *declined* -- Left `comment-variable` even though it repeats part of the definition, because the repetition is harmless and keeps the displayed value column's indeterminate explicit.
