done -- added the GP continuation backslash; checked the broken and fixed blocks under `agents/sage.sh`, and checked the final stored program agrees with the stored $k=3$ value.
done -- added the positive-integer formula for $a_k$; checked the Euler transformation by exact Sage coefficient comparisons for the integer rows $k=1,\ldots,12$.
done -- defined $G$ as the Barnes $G$-function in the Definition, linked the Barnes table from the conjecture comment, and named $f_U(k)$ the random-matrix factor; checked the live audit is clean.
done -- moved the T377 link to the first mention in `rigour details` and left the second mention plain; checked the final API readback.
done -- moved the zeta/Wikipedia citation into the comment and cited Keating-Snaith/CFKRS for the conjecture there; the Definition now says $|\zeta(1/2+it)|$, and the final audit is clean.
