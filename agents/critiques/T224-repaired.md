1. done -- changed the Coxeter-symbol row labels to preserve branch braces and rings, corrected $\overline{DV}_3$ to $[6,3^{1,1}]$, and rewrote `comment-notation`; checked the live draft still had the bad labels, the arXiv TeX has the $6$ edge in that row, Wikipedia lists $\overline{DV}_3$ as $[6,3^{1,1}]$, and Sage still matches all 23 source decimals.
2. done -- replaced the row check-decimal comments with Witt symbols plus closed forms where Kozma-Szirmai Table 1 gives them, adding the Catalan and Bianchi entry links; checked Table 1's formulas, Sage multiplier checks against the live values, T175 for Catalan's constant, and T221 for the $D=-3,-4$ covolumes.
3. done -- rewrote `comment-range` to define noncompact and Koszul-type without repeating `complete-note`; checked the live draft still had "This fill" and "not listed yet" before the change.
4. done -- changed the growth-rate relation to the same finite-volume family with compact tetrahedra there and noncompact tetrahedra here; checked authenticated T223 has nine compact entries and T224 has the 23 noncompact entries, and did not publish because this run may not publish.
5. done -- changed the Kozma-Szirmai bibliography from an arXiv preprint to the Acta Math. Hungar. journal citation; checked Crossref for the DOI, volume 177, issue 2, pages 419-449.
6. done -- changed the visible parameter display to `symbol` and narrowed the constraint text to avoid the triple repetition; checked this does not change the parameter key or order.
7. done -- added the integer-multiple guard to `Lambda` in `Programs` and the generator; checked in Sage that `Lambda(0)` and `Lambda(pi)` return 0 and the program still returns $\mathrm{Vol}([3,3,6])$.
8. done -- rewrote `rigour details` so it no longer says "Before the draft was filled" and names the Clausen and Bianchi rows it checked; checked the same Sage source-decimal, T175, and T221 comparisons.
9. done -- cited `WikiParacompact` in `comment-notation` and removed unused `WikiUniform`; checked the standalone validator reports no unresolved citations.
10. done -- added to `comment-notation` that the row order follows the commensurability classes of Kozma-Szirmai Table 1; checked the source table is grouped by those classes.
11. declined -- the definition sentence is grammatical and says the table's value without ambiguity, so I left it unchanged.
