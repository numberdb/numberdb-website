1. done -- added analytic continuation in $a$ to the Definition after checking the divergent negative-half row against the generator self-check and verifying all 600 stored values still match.
2. done -- removed the principal-branch language and replaced it with the unique continuation in $a$; checked the rendered comment no longer names a nonexistent branch.
3. done -- linked the upper incomplete gamma table and the exponential integral table in `comment-pole`; checked the upper table is visible to `zeta3`, the exponential-integral slug is public, and the repaired render has both links.
4. done -- scoped the rigour-details checks to the rows they cover; checked `self_check()` and `verify(sample=None)` under Sage, and read the rendered note.
5. left for a person -- T351 is still private while T352 links it, so review should publish T351 first or publish the pair together.
6. left for a person -- the regularized incomplete gamma and chi-squared keywords may be useful search bridges, and T351 carries the same pair.
7. done -- removed the repository-only run commands and the hard-coded `NUMBERDB_ASSISTED_BY` default; verified the live raw attachment matches the repository generator byte-for-byte, and left the private-import note unchanged because it was marked noting only.
8. done -- moved the broadest checks before the fold while repairing finding 4; checked the rendered page shows the Kummer and recurrence checks before `more`.
