1. done -- replaced raw `\Sha` with `\text{Sha}` in the BSD formula and rigour note; checked the live API document and owner-view render still had raw `\Sha`, checked the local MathJax bundle/config had no Sha macro, and checked T69/LMFDB usage before changing it.
2. done -- regenerated entry comments so single-term $h(x)$ is written as $y$, $xy$ or $x^3 y$ rather than `(1)y`, `(x)y` or `(x^3)y`; checked all 41 generated comments under Sage before writing and scanned the live document and render afterward.
3. done -- removed the "curves in this range" sentence from generated entry comments; checked it was still present in the live draft and that the regenerated comments keep the representative curve equation without the database-coverage sentence.
4. done -- defined $\Omega_A$, $R_A$, $c_p$, $\text{Sha}(A)$ and $A(\mathbb{Q})_{\mathrm{tors}}$ in the BSD formula; checked the LMFDB BSD knowl's factor list and formula before changing it.
5. done -- changed the rigour note to use $f,h$ for the curve model and to say PARI's `[P,Q]` is the model $[f,h]$; checked the current page used $P,Q$ for the curve while formula (2) also used $P_p$ for Euler factors.
6. done -- narrowed the even-conductor comment to the visible scope and moved the method caveat into rigour details; checked PARI's `lfungenus2` documentation says the model must be minimal at $2$ and the valuation at $2$ may be incorrect for even conductor.
