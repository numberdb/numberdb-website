1. *left for a person* -- the live table still combines the three q-Catalan families, and actually splitting it into replacement drafts is a table-creation decision; I only made the single-table surface less false by changing the value header to "Catalan polynomial" and adding explicit row labels.
2. *done* -- added `Parameters.specialisation.values`, changed the visible parameter label to "convention", and rewrote the comments, completeness note, and rigour details so the reader sees the polynomial names rather than the `qt`, `carlitz`, and `macmahon` keys; preview showed the new row labels.
3. *done* -- shortened the Definition and added a `statistics` comment defining the area-sequence, area, and dinv convention; before writing it I checked in Sage that this enumeration matches Sage's `qt_catalan_number` and every stored `qt` row, and that the stored Carlitz-Riordan and MacMahon rows match Sage.
4. *done* -- cited `GarsiaHaiman` from the Definition; the preview with References present resolves the citation and the final audit is clean.
5. *done* -- deleted the `small-cases` comment, leaving the small omitted values in `formula-small` and the completeness note.
6. *done* -- extended the Sage program with the Carlitz-Riordan and MacMahon calls and ran the exact calls under Sage; I left the extra formula, neighbour, and tag suggestions alone because the critique marked them as notes rather than faults.
