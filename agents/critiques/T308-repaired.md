done -- Replaced both Program snippets with versions that return $W_D$; checked the PARI transform against all 100 live entries and checked the Sage factor selector for $D=-23,-55,-175$ before writing.
done -- Added a PARI-normalisation comment explaining the reciprocal/sign convention; checked on all 100 live entries that `polclass(D,1)` transforms to the stored $W_D$.
done -- Cited `YuiZagier1997` in the Definition and `Cox` in the discriminant comment instead of leaving them uncited; checked the live references and rendered preview chunks before writing.
done -- Rewrote the Ramanujan similar-table relation to state $\omega_D=2^{-1/4}G_n$ for $n=-D$; checked the linked table's live definition and the eight overlapping rows, with largest residual about $2.3\cdot10^{-88}$.
