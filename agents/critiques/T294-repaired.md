1. done -- the original `agents/critiques/T294.md` was absent, so I used the live audit finding about `class` as the actionable proposal: added to the Definition that the row identifiers are parts of one curve label, after checking all 585 entries reconstruct the LMFDB label from $D$, conductor, class and curve; the audit still reports the same shape detector because it does not inspect that exception.
