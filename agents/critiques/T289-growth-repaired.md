1. done -- Added the edge-size $k=2$ minimisers for $3\leq r\leq12$, changed the $k$ constraint and completeness note, and checked them with the generator against T156's comment digits and half-threshold values before sending; `preview(overwrite=False)` said 10 added and 42 left alone, `verify(sample=None)` then matched 52/52, and the authenticated audit is clean after shortening the Definition it flagged.
2. left for a person -- Did not square the $k\geq3$ box to $3\leq k\leq12$, $2\leq r\leq12$, because the report is right that this should move T273 at the same time; T273 still holds only $3\leq k\leq8$, $2\leq r\leq8$, so extending T289 alone would create comments pointing to missing T273 entries.
3. left for a person -- Did not take the further $r\leq20$ growth pass, because after the graph column the remaining range choice is about which rows are worth having rather than a correctness repair.
4. declined -- Kept the per-row comments, because at 52 entries they still point each minimiser to the adjacent threshold table entry, and the report itself says they earn their place at the present size.
5. declined -- Kept 100 digits, because the report says more digits are not a useful growth direction and the generator/audit checks support the existing precision.
6. done -- Reconciled `generators/core-threshold-minimisers/table.yaml` and `generate.py` with the live repaired title, prose, and new graph-case range, and parsed the YAML after editing.
