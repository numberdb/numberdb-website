1. *done* -- added the $D<0$ $\log\Gamma$ formula and the $D=-3$ row comment, and checked the formula in Sage against all 305 live imaginary rows with a wrong-row control.
2. *done* -- added Links for the Dedekind zeta function, the Kronecker symbol and the two OEIS discriminant lists, cited the first two, defined $\gamma$, and checked the Wikipedia pages and local CITE/HREF resolution.
3. *done* -- deleted `comment-log-derivative`, whose statement is already in `formula-laurent`, and checked the remaining changed prose in preview pieces.
4. *done* -- rewrote the $D=-4$ comment so the expression says it equals $\gamma_K$, and checked the $D=-4$ and $D=-3$ special forms in Sage against the live row values.
5. *done* -- sharpened the Similar-tables relations for T128, T145, T34 and T159, and checked T128, T145 and T34 through the live API for the $\kappa_D=L(1,\chi_D)$, Conrey-label and $\eta_0=-\gamma$ claims.
6. *done* -- named the PARI discriminants and the residue relation in `rigour details`, checked PARI at $D=-4,-3,5,12,-995,997$, and checked $L(1,\chi_D)=\kappa_D$ against T128 for all 607 discriminants.
7. *done* -- rewrote the complete-note to name the same 607 fields and simplified the parameter constraint to $D\ne1$; declined changing the harmless terminal periods on ordinary field-name entry comments.
