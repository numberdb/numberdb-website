1. done -- deleted the unsupported Golden ratio similar-table row after checking the live row was still present and the cited omega source did not make that comparison.
2. done -- changed the `branch` parameter to "branch of $W$" with constraints naming $W_0$ and $W_{-1}$, after checking the live page and all stored rows' branch inequalities in Sage.
3. done -- rewrote the Euler's constant $e$ gloss to name the branch point and $W_0(e)=1$, after checking those special values in Sage.
4. left for a person -- changing parameter order would change entry addresses; I left the existing `branch, x` order alone.
5. done -- removed the repeated "$W_0(0)=0$ is not listed" sentence from the comment, after checking the complete-note already says it and the remaining exact-value claims in Sage.
6. done -- added lower-branch examples to both printed programs after running the Sage and mpmath calls; the same-page omega link, citation order, and shorter branch labels were declined as cosmetic.
