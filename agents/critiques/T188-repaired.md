done -- replaced the live external-definition wording with a short scope sentence, moved explicit $\operatorname{Ei}$, $E_1$ and $\operatorname{li}$ integral definitions into Formulas, removed the redundant name and branch comments, and narrowed the rigour note; checked DLMF 6.2 and Sage ball identities over the stored arguments.
done -- replaced comment (10) with a definition of $E_n(x)$ scoped to positive real $x$; checked DLMF 8.19, the change of variables to $E_1(x)$, and mpmath-in-Sage agreement for all 60 stored arguments.
done -- rewrote formula (7) to say principal branch rather than real continuation; checked DLMF 6.4 and Sage complex-ball identities for $\operatorname{Ci}(-x)$ and $\operatorname{Chi}(-x)$ at all 60 stored arguments.
done -- removed the prime-zeta Similar table row; checked the live prime-zeta table definition, comments and formulas, which did not name $\operatorname{li}$, $\operatorname{Ei}$ or a usable relation to this table.
done -- rewrote the Gibbs comment to link `HREF{Gibbs_constant#WG}` for the single number; checked the live Gibbs table row key `WG` and verified $\operatorname{Si}(\pi)$ against that row using NumberDB's decimal-interval convention.
