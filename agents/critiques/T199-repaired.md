1. *done* -- Changed the rigour note and Sage program from `losing=64` to `losing=80`; checked all 270 entries in Sage/arb, where the widest radius was about $1.6\cdot10^{-115}$ at guard 64 and about $2.5\cdot10^{-120}$ at guard 80, and verified the generator still matched 270/270 live entries.
2. *done* -- Replaced the false comparison with the $I_\nu$ table by the zero-limit reason for excluding $x=0$; checked live T196 and T199 both omit $x=0$, and checked DLMF 10.30's limiting forms for $K_0$ and $K_\nu$ with $\nu>0$.
3. *done* -- Removed the 30 repeated $\nu=1/2$ entry comments from the live table and removed `_comment` from `generate.py`; checked the live API now reports zero entry comments and the regenerated values still match all 270 entries.
4. *left for a person* -- T196 is still a draft, checked by anonymous `/T196` returning 404, so the publication-order risk remains; I did not remove the T196 link from T199 because the relation is correct once both drafts are reviewed.
5. *done* -- Replaced the special $K_{1/2}$ formula with the checked half-integer formula using the corpus's Bessel polynomials and rewrote the half-integer comment to cite it; checked DLMF 10.49.12 with the 10.47 spherical-normalisation convention and verified the formula in Sage/arb on all 90 stored half-integer rows.
6. *left for a person* -- Left the definition in the batch's named-function style; checked the same convention in the Bessel siblings, and changing only T199 would be a cross-table consistency decision rather than a local repair.
7. *done* -- Removed `Matern covariance` and `Yukawa potential` from `Keywords`; checked the owner-rendered page had no visible explanation for either phrase and kept the remaining keywords that directly name $K_\nu$ and its aliases.
8. *declined* -- Left the negative-order comment, J/Y zeros and extrema glosses, and principal-branch sentence otherwise unchanged; checked the owner render and audits, and these are harmless or batch-level wording choices rather than faults that need a T199-only edit.
