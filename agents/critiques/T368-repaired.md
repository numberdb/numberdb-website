1. done -- deleted the thirteen request links unrelated to Charlier zeros, kept Request11, and checked the live Links plus the current GitHub issue titles before changing them.
2. done -- added the Charlier three-term recurrence from T267 as `formula-recurrence`, and checked T267 plus the generator recurrence before writing it.
3. done -- added a rational-root comment for the $n=1$ and $x=1$ cases, and checked in Sage that $C_1(x;a)=1-x/a$, that $C_n(1;a)=1-n/a$ on the full grid, and that all 16 exact stored values are exact rational roots.
4. done -- removed the polygamma similar-table line after checking the live relation was still only "another table of indexed real zeros".
5. done -- deleted `comment-zero-only` after checking it was still only an apology for absent quadrature weights.
6. done -- changed `$AA$` to `` `AA` `` in `rigour details`, after checking the live field still used the Sage ring name as mathematics.
7. done -- removed "Before the draft was offered" from `rigour details`, after checking the remaining sentence still describes the actual verification.
8. done -- added T267's Poisson-Charlier keywords, after checking T267 currently carries them and T368 had no `Keywords`.
9. declined -- the normalisation wording is understandable once the recurrence is present, and putting `HREF{Charlier_polynomials}` in the Definition would risk the known audit refusal for table links in definitions.
10. declined -- the golden-ratio comments would be a nicety rather than a repair, and T32 does not store the squared values as entries.
