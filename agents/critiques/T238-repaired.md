* *done* -- Replaced `comment-index` with a graph6 explanation, added the `Graph6` link, and changed the Sage program to decode and produce the table's graph6 names; checked the live draft still had only the old index sentence and rendered the updated comments and program through `/preview?table=`.
* *done* -- Added the parameter title `graph` and narrowed the constraint to connected simple graphs named by canonical graph6 string; checked the live parameter still carried the computed range and the preview rendered the updated parameter block without an error.
* *done* -- Renamed `D]{`, `ELrw`, and `FIefw` to $W_5$, $W_6$, and $W_7$; checked in Sage that `graphs.WheelGraph(5)`, `(6)`, and `(7)` canonicalise to those three keys.
* *done* -- Added the T237 Similar tables row in T238; checked the slug resolves through the API and verified $\lambda(G,x)=(-1)^n\det((k-x)I-A(G))$ on all 16 regular graphs shared by T237 and T238. The reverse T237 edit was left out because this repair was for T238.
* *done* -- Rewrote the 119 cospectral entry comments and `comment-cospectral` without backticked keys; checked the stored values give 934 distinct polynomials, 52 pairs, 5 triples, and 119 graphs in shared classes, and previewed rows including one whose key contains a backtick.
* *done* -- Rewrote the zero-root comment to state the simple factor and algebraic connectivity; checked every stored polynomial has exactly one factor $x$.
