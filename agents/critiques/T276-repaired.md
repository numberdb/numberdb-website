1. done -- shortened the definition to say the MAP threshold is the binary-erasure-channel threshold below which MAP decoding succeeds with high probability as blocklength tends to infinity; checked KRU's threshold description and MMU's conditional-entropy definition, then re-audited after the first longer version was refused by the audit.
2. done -- rewrote `comment-root` to say that at $\epsilon=\epsilon^{\mathrm{MAP}}_{l,r}$ there are two fixed points and that $x^{\mathrm{MAP}}_{l,r}$ is the larger stable Maxwell root; checked all 45 rows under Sage.
3. done -- rewrote `comment-maxwell` and added `formula-maxwell-area` to state the EXIT-area identity; checked the derivative identity and endpoint symbolically for every $(l,r)$ in the table.
4. done -- changed `formula-trial-entropy` into an identity for $p^{\mathrm{MAP}}_{l,r}(x)$ and removed it from `rigour details` as an independent check; checked the polynomial identity for every row.
5. done -- replaced the copied convention and $l=2$ comments with the corrected design-rate, Shannon-limit, near-$l/r$, and cycle-ensemble statements; checked T275's live wording, KRU Lemma 8's fixed-design-rate scope, the table's gaps, and the $l=2$, $3\leq r\leq12$ behavior under Sage.
6. done -- completed the MMU reference with the IEEE Transactions on Information Theory volume, issue, pages, year, and the first author's accent; checked the bibliographic details against KRU's reference list.
7. done -- added `BEC`, `erasure threshold`, and `optimal threshold` to `Keywords`; checked T275's live keywords for the sibling terminology.
