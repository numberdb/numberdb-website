1. done -- Rewrote the Definition to state the minimised function and define $Q$ in place, without putting the proposed cross-table HREF in the Definition; checked the derivative identities in Sage over all 42 stored $(k,r)$ pairs.
2. done -- Replaced the companion table's copied keywords with `random hypergraph core, Poisson parameter, core threshold minimiser` after re-reading the live draft and the companion table.
3. done -- Retitled the draft to `Minimisers in the core threshold formula of random $k$-uniform hypergraphs`, after checking the live title was still the old one.
4. done -- Corrected the graph-table caption to `$k$-core thresholds of the Erdős–Rényi random graph`; checked the linked graph table's title through the API.
5. done -- Replaced the repeated threshold formula in `comment-convention` with a reference to `CITE{formula-threshold}` and printed the prose before sending it.
6. done -- Shortened `formula-minimiser` to the relation defining $g$ and the displayed $g'$; checked the derivative numerator and $g'$ symbolically in Sage for every stored $(k,r)$.
7. done -- Reworded `rigour details` to avoid the circular "unique minimiser" wording and the ambiguous "$k=2$ extension"; checked the edge-size-$2$ thresholds against the graph table for $3\leq r\leq8$.
