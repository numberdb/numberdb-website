1. *done* -- changed the value-column header to name the stored shifted forms, after checking on all 958 entries that the comments parse as the shift exponent $a$.
2. *done* -- changed the 822 bare `a=...` entry comments to `$a=...$`, after checking every such comment matched the shift-comment pattern.
3. *done* -- added the change of variables between the $(v,z)$ and $(l,m)$ normalisations to `Formulas`, after checking all 479 normalisation pairs exactly over `QQ(i)`.
4. *done* -- moved the skein relations and shift rule into `Formulas` and added the Conway, Jones and Alexander specialisations, after checking 479 Conway, 479 Jones and 479 Alexander specialisations against the sibling tables.
5. *done* -- rewrote the Definition without parameter keys and with Rolfsen and Perko cited; the proposed "nonzero constant term" wording was narrowed because a Sage check found shifted entries with zero constant coefficient, so it now says "shifted into ordinary polynomial form".
6. *done* -- cited `Rolfsen` in the Definition, after confirming the reference was still declared and otherwise uncited.
7. *done* -- removed the size/design sentence from `rigour details` and kept only computation and checking details, after checking the normalisation substitution and mirror identities on the stored rows.
8. *done* -- replaced "the reciprocal substitution in the first variable" with the explicit mirror identities $P_{\bar K}(v,z)=P_K(v^{-1},z)$ and $P_{\bar K}(l,m)=P_K(l^{-1},m)$, after checking 458 mirror reciprocal identities.
9. *done* -- changed the `normalisation` parameter title and constraint to name the two standard normalisations rather than Sage as their source, after rereading the rendered parameter text in the live document.
10. *done* -- changed `comment-range` to name $n$ and $k$ rather than pointing at "the two integer parameters", after reading the whole paragraph for pronoun seams.
11. *done* -- changed the `Programs` snippet comment to match the stored term order for the trefoil entry, after comparing it with the live row.
12. *declined* -- the entry block size is under the soft limit and the range is fixed by the Rolfsen table; the existing range comment already says why eleven-crossing knots are outside this table.
