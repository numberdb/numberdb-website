1. *done* -- Rewrote every partition parameter value in the live draft as `(2,1)`-style keys, updated the attached generator to emit and parse that spelling, and checked that the 133 colliding identities/394 affected rows became 0 collisions over the same 1197 rows; the generator integrity checks and full `verify(sample=None)` then passed.
2. *done* -- Added the `numberdb.sage` initializer, the lazy `sage.rings.polynomial.laurent_polynomial_ring` import, and an explicit `print` to the Sage program; checked the old snippet raised the reported Sage import error and the new snippet prints `q`.
3. *left for a person* -- The live table still has 555 rows equal to `1` or a power of `q`, 61 more equal to `q + 1`, and 100 distinct polynomials, but thinning or changing the range is a reviewer decision rather than a clear repair; I left the range and completeness note unchanged.
4. *done* -- Cited `CITE{Hall}` from the Definition where the Hall polynomial is named; checked the live document had the Hall reference uncited and checked Hall's paper is the standard source for the Hall polynomiality result before adding it.
5. *done* -- Linked the first formula-section mention of the Hall-Littlewood $P$ basis to `HREF{T261}` and changed the Kostka-Foulkes relation from "the same" to "the"; the final rendered page contains the link and the API audit is clean.
6a. *done* -- Removed the repeated edge-case sentence from the convention comment; the edge cases remain stated in `complete-note` and `formula-edges`.
6b. *declined* -- Left the `Similar tables` targets as `HREF{T261}` and `HREF{T259}` because both targets are drafts and no public slug is available to read off yet.
6c. *declined* -- Did not add the module-type clause to the Definition, because it would lengthen the definition and the critique marked it as optional rather than a fault.
