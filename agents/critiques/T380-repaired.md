1. done -- deleted `Request20` through `Request136`; checked the live table still had them and the GitHub issue titles were unrelated, and did not add #175 because the skill says requests should leave no trace in the table.
2. done -- removed the false "integer normalisation" prose and added the factorial normalisation $g_{USp}(k)=\left(k(k+1)/2\right)!\,f_{USp}(k)$; checked T379's formula and verified its symplectic values in Sage, where every stored row is rational but nonintegral.
3. done -- added a formula defining $f_{USp}(k)$ as the symplectic random-matrix factor and giving the positive-integer $g_{USp}$ conversion; checked T379's definition/formula before narrowing the conversion to positive integer $k$.
4. done -- removed the T379 similar-table row instead of only recaptioning it, because live `/T379` is 404 while `api/table?id=T379` shows a draft and the audit flagged the public draft link.
5. done -- rewrote the moment formula as an averaged asymptotic over fundamental discriminants and defined $\chi_d$ there; checked the rendered formula and the clean audit afterwards.
6. done -- replaced the corpus-internal "same key" comment with prose using $k$ and a verified HREF to T377's live slug.
7. done -- added an OEIS A065463 link and changed the $k=1$ comment to cite it; checked the OEIS b-file's 98 digits against the stored value and verified the $k=1$ local-factor collapse in Sage.
8. done -- changed the title to include $A_k$, matching the value column and the sibling table style.
9. done -- removed the unsupported `Keating-Snaith` keyword rather than adding an uncited name to the page.
10. done -- rewrote the definition so CFKRS is cited before the Dirichlet-L link; checked the rendered references now read `[1] [2]`.
11. done -- removed all T379 links from the public table because T379 is still a draft; `GET /api/table/T380/audit` is now clean.
