done -- Narrowed `complete-note` so Sac-Épée's random-sampling search is scoped to Salem degree $64$; checked the live T284 text still had the unscoped sentence and checked arXiv:2409.11159v3 before changing it.
done -- Linked the first mention of the plastic constant in `comment-bound` to T286 entry `1` and added T286 to `Similar tables`; checked the live T286 row and verified in Sage that its stored interval contains the real root of $x^3-x-1$.
declined -- Did not add the optional `Known180.gz` clause, because I did not recompute that source on this run and the repair needed only the narrower Sac-Épée claim.
left for a person -- Did not extend or retitle the table to $49/37$; that changes what T284 is rather than repairing a fault, and the critique itself leaves it as the owner's call.
declined -- Did not add the neighbouring-family or more-digits notes from "What would not be growth"; they are critique analysis, not facts this table needs to state.
