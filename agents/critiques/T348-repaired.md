1. done -- Removed the 31 rational rows by rerunning the corrected generator; checked the proposed screen and five isolated rows in Sage with symbolic simplification and 7000-bit ball containment, then verified the final generator against the draft.
2. done -- Deleted `Links.Requested129`; checked the live document still contained it and that no prose cited it.
3. done -- Cut the build-note clause from `comment-branch`; checked from the live parameters that $z<1$ keeps the principal values real and left the computation check in `rigour details`.
4. done -- Removed `comment-rational-rows` and moved the actual grid and rational-omission statement into `complete-note`; checked the final note against the 945-row grid and the 730 generated rows.
5. done -- Rewrote the definition as sentences and left the $b\geq a$ indexing convention in the parameter constraint and symmetry comment; checked the changed paragraph before sending.
6. done -- Replaced the positional “specialisation” relations with the explicit $K(m)$ and $E(m)$ hypergeometric formulas from `formula-elliptic`.
7. declined -- T348 already names T178 as the Ramanujan signature subfamily, and the duplicate rows are shared by definition rather than a repeat of T178’s computation; I left `repeats` unset.
