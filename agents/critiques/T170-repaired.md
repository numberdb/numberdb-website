1. done -- replaced the live Definition with a power-mean definition of $K_p$ that explicitly keeps $M_0$ as the geometric case; checked the live API document and owner-rendered draft page still lacked this, verified every stored $K_p$ row against direct Gauss-Kuzmin power-mean sums with positive tail bounds, and reran `audit_table T170` and `audit_table T170 --links` with nothing to report.
2. declined -- left the "Noting only" items unchanged because the critique itself treats them as stylistic observations rather than repairs; checked the live API document, owner-rendered draft page, and clean audit output before declining them.
