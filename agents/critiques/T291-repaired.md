Audit finding: declined -- the audit still reports the long Definition, but the normal-form list is part of the convention and the only audit finding is the same one the critique declined.
1. done -- deleted the `suspension` comment after rendering confirmed the duplicate `suspension` blocks; the suspension formula remains.
2. done -- changed the Definition to reduced middle homology $\tilde H_{n-1}$; Sage checked that the $A_k$, $n=1$ rows for $1\leq k\leq12$ match reduced $H_0$ and not unreduced $H_0$.
3. done -- defined $\Phi_k(t)$ in the notation comment and linked `HREF{Cyclotomic_polynomials}`; the slug was checked against the live page.
4. done -- added the Alexander-polynomial Similar tables entry; Sage checked the six stored equalities with T138 at $3_1$, $5_1$, $7_1$, $9_1$, $8_{19}$ and $10_{124}$.
5. done -- changed the Milnor algebra in the notation comment to $\mathbb{C}[x_1,\ldots,x_c]/(\partial f)$ and added that $c$ is the corank in the Definition; the formula already used $x_1,\ldots,x_c$.
6. left for a person -- `HREF{T290}` still points at a draft, and whether to publish T290 before T291 is a review-order decision rather than a table repair.
7. declined -- I did not rewrite the eigenvalue formula prose or strengthen the Singular-check wording; the formula states a relation, and I did not rerun Singular to justify changing "Checks compare" to "every row agreed".
8. declined -- the Coxeter relation is true as written, and the extra explanation through Coxeter degrees is useful background rather than a correction.
