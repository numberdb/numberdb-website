1. *done* -- Regenerated shared-value comments without Markdown code spans, using `&#96;` only where graph6 keys contain backticks; checked the live preview, that the payload has 26 entity-escaped entry comments and no raw backticks in entry comments, and that `verify(sample=None)` still matches all 987 entries.
2. *done* -- Replaced the partial hard-core entry link with the whole entropy-constants table and stated the relation using activity 1; checked that `?entry=hard-core` still warns, that `?entry=hard-core,line,kappa` resolves, and that T153 contains the `hard-core/line/kappa` row and line formula.
3. *done* -- Changed generated path comments to use `$P_{n}$`; checked the payload has no `$P_10$`, `$P_11$` or `$P_12$` comments and previewed the repaired rows.
4. *done* -- Rewrote `comment-graph6` to give `T.canonical_label(algorithm='sage').graph6_string()` and `Graph(name)`; checked every live T240 key is recovered by Sage and equals the `algorithm='sage'` canonical graph6 string.
5. *done* -- Replaced the tree-only definition with the finite-simple-graph definition and dropped the redundant storage sentence; checked all 987 live rows against Sage's complement clique polynomial.
6. *done* -- Replaced the subset-enumerating Sage snippet with `print(G.complement().clique_polynomial())`; ran the snippet under `agents/sage.sh` and got the stored row for `F@HSO`.
7. *done* -- Deleted `comment-range` and expanded the completeness note to say the table holds 987 trees through 12 vertices and that the 1301 trees on 13 vertices exceed the soft entry limit; checked Sage's tree counts for 1 through 13 vertices.
8. *done* -- Replaced the chromatic and Tutte Similar-tables relations with the tree formulas under the same keys; checked every T240 tree through 7 vertices is present in T125 and T126 with chromatic polynomial $x(x-1)^{n-1}$ and Tutte polynomial $x^{n-1}$.
