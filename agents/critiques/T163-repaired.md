1. done -- replaced the vague constellation range in the comment and complete note with every prime constellation for $3\leq k\leq 12$; checked the live draft and exhaustively found the least-diameter admissible tuples for each $k=3,\ldots,12$, with no mismatch against the 21 stored rows.
2. done -- deleted the duplicate `MathWorldConstellation` reference while keeping and citing the Links entry; checked the rendered page no longer shows the out-of-order duplicate reference and `audit_table T163` reports nothing.
3. done -- replaced the negative "not ten times A269843" sentence with the positive tail-factor statement; checked the local factors at $p=2,3,5$ give $50625/2048$ against A269843's $p>5$ tail.
4. done -- added OEIS citations to the six $k=10,11,12$ row comments and removed the unreadable finite-factor fractions from those comments; checked each OEIS link title matches the stored offset tuple and the comments still say the finite factor of CITE{formula-tail} is included.
5. done -- changed generated row comments from non-prime lower bounds $p\geq d+1$ to the formula's $p>d$, changed "normalisation" to "normalization", removed the "named tail" and "intentionally rectangular" wording, and removed the misleading `OEISConstellations` link; checked the rendered page for the stale strings.
added 4. done -- added a comment explaining reflection invariance; checked every stored mirror pair has the same value and the same residue counts $\nu_p(H)$ for all primes $p\leq 47$, and used the residue-count argument for all primes.
added 5. done -- used the existing pair formula and added a comment saying pair values depend only on the odd prime divisors of $D$; checked every even $D\leq 100$ against $2C_2\prod_{p\mid D,\ p>2}(p-1)/(p-2)$ and grouped the stored values by those odd prime divisors with no mismatch.
