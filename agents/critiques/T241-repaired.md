1. done -- added $h(F(2,m))=1+2/m$, stored all ten $F(2,m)$ nats rows and the unit-variance hyperbolic secant bits row exactly, and checked the cancellation and exact rows in Sage before writing.
2. done -- added closed forms for the shaped families, Beta symmetry, and entry comments for the unshaped distributions; spot-checked the Gamma, Pareto and Beta formulas in Sage and rendered the formulas through preview.
3. done -- moved the computed range and its reason into `complete-note`, deleted the separate range comment, and replaced the omitted-row comment with mathematical facts checked against the stored rows.
4. done -- deleted the two unrelated entropy links and the error-function link, narrowed the Gamma and Beta-function relations, and rendered the remaining similar-table links through preview.
5. done -- added entry comments naming the Cauchy, half-normal, Rayleigh, Maxwell-Boltzmann and arcsine cases while keeping `equals`; the API readback and generator verification confirmed the rows.
6. done -- rewrote the standard-forms comment, relabelled the hyperbolic secant densities, renamed `hypsecant-scipy` to `hypsecant` while still a draft, and changed the label to Lévy with `Levy entropy` in Keywords.
7. done -- reworded the scale formula, normal entropy bound and lognormal comment, and added `lognormal entropy`; the edited prose rendered cleanly in preview.
