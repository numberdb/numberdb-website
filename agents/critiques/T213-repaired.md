1. done -- sorted the live rows and generator by conductor, class letter, numeric discriminant and curve number; checked the live API/page still had the twelve misordered classes and verified the final document has zero such classes.
2. done -- replaced the undefined Neron-basis wording with a shorter definition plus a `comment-lattice` normalisation; checked the current LMFDB real-period knowl for the canonical-sheaf regular-model basis and homology-cycle construction first.
3. done -- added the `period` tag; checked T72, T73 and T74 currently carry `period`.
4. done -- rewrote the isogeny comment with same-table links; checked the two 249.a rows have the same period and the two 277.a rows are in one class with different periods.
5. done -- cited `LMFDBData169` in `rigour details`; checked it was previously uncited and that the linked JSON exposes the `real_period` field.
6. done -- removed the duplicate source-precision comment and put "truncated and not rounded" in `rigour details`; checked the LMFDB JSON records the source value as a 97-bit real literal.
