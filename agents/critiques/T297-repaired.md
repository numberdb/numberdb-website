1. *done* -- restored the product definition of $G_n$ and kept the two-normalisation note; checked the q-product and the stored values with `agents/sage.sh`.
2. *done* -- kept the even-$n$ $G_n$ rows, added the T295/T296 range reason to `complete-note`, and added a caveat that $g_n$ is different and not stored; checked $g_n$ against $G_n$ at $n=2,4,6,10,58$ in Sage.
3. *done* -- changed both parameter titles so the rendered parameter lines no longer repeat "positive integer" or "convention".
4. *done* -- named and linked T295 and T296 in the CM-point comment and added T295 to `Similar tables`; checked T295/T296 live and checked $G_n^{-24}=4m_n(1-m_n)$ for $n=1,\ldots,100$ in Sage.
5. *done* -- deleted the comment that named `ramanujan` and `weber` by key and changed `rigour details` to say "$G_n$ entries".
6. *done* -- kept the title's $\mathfrak f(\sqrt{-n})$ notation and defined it in the Definition as Weber's notation for $\mathfrak f(i\sqrt n)$.
7. *done* -- rewrote the reciprocal formula using $G_x$ for real $x>0$; checked $G_{1/n}=G_n$ for every stored integer $n$ in Sage.
8. *declined* -- the closed-form row comments are optional and would be a separate source-backed annotation pass, not needed to repair the table.
9. *done* -- sharpened the T83 relation to the rational singular-modulus rows with $\Delta=-4n$ for $n=1,2,3,4,7$; checked T83's live entries and the generator's $j$-formula check.
10. *declined* -- the program's extra printed guard digits are harmless, and the generator verification matched all 200 stored entries.
