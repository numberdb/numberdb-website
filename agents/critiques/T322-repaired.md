1. done -- changed `comment-known` so the even case is existential, after checking Willms's abstract, Wikipedia's length-1 wording, and a Sage enumeration of normalized words through length 20.
2. done -- changed `rigour details` from polynomials over `$\mathbb Z[x]$` to polynomials over `$\mathbb Z$`, after checking the table type is `Z[]` and the stored sign words reproduce the eight polynomials.
3. done -- replaced the muddled Barker-bound sentence in `formula-l4-merit` with `$F=169/12$` for the length-$13$ sequence, after checking all eight autocorrelation lists and merit factors in Sage.
4. left for a person -- the two draft `Similar tables` targets are a publishing-order constraint, not a T322 text repair; the live audit is clean.
5. done -- named the trivial length-$1$ sequence and changed the `$n\geq2$` constraint to explain that the normalization needs two terms, after checking length $1$ is vacuously Barker.
6. done -- rewrote `complete-note` to say the eight listed sequences have lengths `$2,3,4,4,5,7,11,13$`, after checking the live rows have exactly those lengths.
7. declined -- `comment-coefficients` is duplicative but correct, and the critique judged the repetition a defensible kindness.
8. done -- removed the `numberdb-data#160` trace and local `agents/sage.sh` publish command from `generate.py`, then ran the modified generator and got `8/8 matched`.
9. declined -- the `Programs` snippet is narrow but correct, and replacing it with a brute-force search would be a usefulness improvement rather than a repair.
10. declined -- the duplicate arXiv/DOI links on the Willms reference are harmless and do not make the citation wrong.
