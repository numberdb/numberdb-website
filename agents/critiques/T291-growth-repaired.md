1. done -- added the missing opposite-parity rows for the seven corank-3 labels, after checking the suspension identity in the generator and all 178 widened rows against Singular's `spectrum`.
2. done -- extended $A_k$ and $D_k$ through $k=14$, after the widened generator verified the 87 old rows unchanged and the full 178-row table matched its formula.
3. done -- added the 37 hyperbolic $T_{p,q,r}$ labels with $\mu\leq14$ as 74 rows, narrowed the modulus convention to the parabolic and exceptional families, added the hyperbolic closed form, and checked every new row against Singular.
4. declined -- did not extend beyond $\mu\leq14$, because the report's own measurements make that the coherent bound matching the sporadic labels already present.
5. done -- used underscore-separated keys such as `T2_3_10`, added their display labels, and confirmed the live table now has 82 labels and 178 entries.
6. left for a person -- did not extend T290 in this T291 repair; the report itself says T290 needs an extra generic-modulus check for `bfct` before moving the pair together.
7. done -- treated the no-competition finding as support for adding the rows, and rechecked `api/search` for `singularity`, `monodromy`, `Milnor`, and `hyperbolic`, all of which still returned no public competing table.
