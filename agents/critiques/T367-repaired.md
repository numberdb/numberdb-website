left for a person -- `agents/critiques/T367.md` is absent from this checkout and from local git history, so no original finding could be checked or acted on; checked the authenticated live API document for draft T367 (`Refractive indices of the Schott optical glasses`), confirmed unauthenticated `/api/table?id=T367` and `https://numberdb.org/T367` return not found while the keyed draft read has 935 entries with only `Schott, SF6G05, g` omitted as the completeness note says, and the keyed audit reports the pre-existing singleton tag finding `Tags: "optics" reaches only this table; a tag that leads nowhere else leads nowhere`.
