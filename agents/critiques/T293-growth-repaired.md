declined -- did not grow T293; the live API document and rendered page still show 1171 entries, and `agents/sage.sh /tmp/t293_counts.py` confirmed the pinned ecnf-data counts $N\leq250=1171$, $N\leq254=1195$, and $N\leq255=1213$.
declined -- did not add a size exception; the live table is under the soft limits and the keyed audit after the repair returned clean.
declined -- kept the entry comments; the same source check showed entry count, not bytes, is the binding limit, and the comments identify the curve data not otherwise present in the row.
declined -- did not split by rank; `agents/sage.sh /tmp/t293_counts.py` confirmed 1163 rank-$1$ rows and only 8 rank-$2$ rows, so a rank split would not make a useful pair of tables.
done -- changed `complete-note` to say the $N\leq250$ cutoff reaches the first curves of rank $2$ among the six fields, at conductor norm $235$ over $D=21$; `agents/sage.sh /tmp/t293_counts.py` checked the first rank-$\geq2$ norms and the $N\leq255$ count before the API write.
declined -- left the ragged per-field counts under the uniform conductor-norm cutoff; `agents/sage.sh /tmp/t293_counts.py` confirmed the first positive-rank norms and the first rank-$2$ norms, so an equal-depth replacement would lose the rank-$2$ rows.
declined -- did not split by $D$; $D$ names the field the regulator is of, the live value header remains $R_{E/K}$ for every row, and any per-field split is a person's range decision rather than a repair.
left for a person -- did not grow the companion special-$L$-value table from a T293 repair; `agents/sage.sh /tmp/t293_companion.py` confirmed it has room, but that is T292 work rather than a T293 defect.
done -- changed the `Similar tables` relation for the special-$L$-value table from "the same ecnf-data curves" to "elliptic curves from the same ecnf-data source"; `agents/sage.sh /tmp/t293_companion.py` checked the live ranges, fields, ranks, and first labels differ.
