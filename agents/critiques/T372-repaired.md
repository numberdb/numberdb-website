1. done -- rewrote `Programs[program-sage].code` with real newlines and `q[51]`; Sage checked that the stored code now reproduces `q_50` and computes `q_51`, while the pre-repair literal-`\n` code failed as syntax.
2. done -- added `comment-standardization` linking T371 and stating $q_n(x)=2^{-(n+1)/2}q^H_n(x/\sqrt{2})$; Sage checked the identity exactly for $0\leq n\leq50$ and the new slug answered 200.
3. done -- replaced the T134 relation with the conversion $y_k=\sqrt{2}\,x_k$ and $q_n(y_k)/He_n'(y_k)=w_k/\sqrt{\pi}$; Sage checked the $n=2$ conversion against T134's stored physicists' nodes and weights.
4. done -- removed the uncited `Request` link after confirming the issue was not cited by the table prose.
5. done -- rewrote `complete-note` as a sentence clause and linked T104; the API read confirmed T104 holds exactly $n=0$ through $50$.
6. done -- rewrote `rigour details` and `comment-hermite` so the checks are stated as completed work and the degree/parity claims apply only for $n\geq1$; Sage checked degree, parity-as-a-function and monicity for $1\leq n\leq50$, with $q_0=0$.
7. done -- rewrote `comment-density` to avoid opening with a citation marker and to say that Wikipedia defines the construction for an arbitrary density without choosing one.
noted only, parameter title: declined -- no title was added, because calling $n$ the degree would be wrong for $q_n$.
noted only, definition closing clause: declined -- the repeated naming of the density was left because the critique judged it to match the family pattern.
