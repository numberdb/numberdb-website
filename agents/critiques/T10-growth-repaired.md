done -- left the table at the two reciprocal entries after rereading live T10; the definition has no index, and the two forms are invertible conventions.
done -- checked the recorded union method in Sage: the 2014-2020 measurements reproduce both stored intervals, and adding CODATA 2022 leaves both strings unchanged.
done -- kept the existing interval widths rather than narrowing to the T12 convention; Parker 2018 and Morel 2020 are in tension, so the union convention is now recorded in `complete-note` and `rigour details`.
done -- added `complete: yes` and a completeness note after verifying no live `complete` field existed.
done -- replaced the attached `generate.sage`; the downloaded old file failed under `agents/sage.sh`, and the replacement downloaded after repair prints the two stored entries.
done -- named the five measurements in `reliability`, added NIST CODATA links and a SciPy `Programs` snippet; NIST and SciPy 1.17.1 both gave CODATA 2022 values inside the stored intervals.
declined -- the audit sampling caveats are site-audit limitations rather than table content; the keyed post-repair audit returned clean, and the `.sage` audit blind spot is recorded in `docs/agent-environment.md`.
left for a person -- the search-ranking issue is corpus search behavior; T10's existing keyword already contains "Sommerfeld constant", and the critique had already proposed the lesson.
done -- added a `Similar tables` link to T12 after checking live T12.
done -- cited `Som16` from a new comment after checking the Sommerfeld-name claim against NIST and Wikipedia source material.
