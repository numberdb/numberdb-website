1. *done* -- braced every stored row label and the generator's three label templates as $B_{n}$, after re-reading the live API document and confirming the rendered draft route was private-only while preview pieces showed the repaired $B_{10}$ labels.
2. *done* -- replaced the two normalisation comments with one definition of $d$, $\Lambda_n$, the relative, normalised and Euclidean volumes, after checking the row conversion identities and the zero-margin lattice covolume in Sage.
3. *done* -- rewrote the formulas to give the direct relative, normalised and Euclidean relations and to define the covolume without $\tau$, after checking all ten rows satisfy $\operatorname{Vol}(B_n)=((n-1)^2)!\operatorname{vol}_{\mathrm{rel}}(B_n)$ and $\operatorname{vol}_{\mathrm{Euc}}(B_n)=n^{n-1}\operatorname{vol}_{\mathrm{rel}}(B_n)$.
4. *done* -- removed the two generic volume similar-table entries, after re-reading the live document and confirming they named only a shared subject already covered by the `volume` tag.
5. *done* -- cited Beck and Pixton in `complete-note` and added the Beck-Pixton volumes link, after checking the arXiv abstract says the paper computed the Ehrhart polynomial and volume of $B_9$ and the Beck-Pixton site announces the $B_{10}$ volume.
6. *done* -- replaced the $n=9$ provenance sentence with the corrected Beck-Pixton explanation, after comparing the stored Euclidean values with Beck-Pixton's volume list and OEIS A078524/A078525.
7. *done* -- replaced `Programs` with a Sage snippet that directly computes the small-$n$ Euclidean volume, after running it under `agents/sage.sh` and checking it returns `176/2835` for $B_4$.
8. *left for a person* -- the n=1,2 rows, keyword choices, publication ordering with T232, and T232's own comment are noted-only or belong to another table; finding 3 covered the first-mention link for the Ehrhart polynomial.
