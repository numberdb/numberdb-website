done -- replaced the quotient-like Kronecker notation in `formula-dirichlet` with `\chi_D=\left(\frac{D}{\cdot}\right)` and checked the live T128/T145 convention and the preview rendering.
done -- defined `\zeta_K`, named and cited Humbert's formula, cited Maclachlan-Reid and the Dedekind-zeta link, and linked T145 for the Dirichlet `L`-function; checked the live Bianchi and Dedekind-zeta sources and preview citation resolution.
done -- rewrote the definition to identify the Bianchi group as `\mathrm{PSL}_2(\mathcal{O}_K)` and its covolume as `\mathrm{Vol}(\Gamma\backslash\mathbb{H}^3)`, then read the changed definition and convention comment together and checked the definition length is still under the audit limit.
done -- rewrote the T128 and T145 similar-table relations and added T162; checked the live T128/T145 completeness notes, T162's Borel-volume comment, and that all three table slugs resolve.
done -- rewrote the figure-eight and Catalan special-value sentence with local row links, external entry links, and a Chu2020 citation; checked Chu's source for the index-12 subgroup statement and checked in Sage that `12*T221[-3]` matches T141 `(4,1)` and `3*T221[-4]` matches T175 `(2,1/2)` to the stored precision.
done -- narrowed the completeness note to say the `|D|\leq 1000` range matches the quadratic Dedekind-zeta residue table; checked T128's live range and counted its 305 negative and 302 positive fundamental discriminants.
