* done -- replaced the "same grid" relation after checking the live companion table says it covers every degree $0\leq n\leq N$ while T369 stops at $n\leq4$.
* done -- recomputed the exact Krawtchouk polynomials in Sage, found 154 rational roots including 73 integers, checked each lay in the stored interval, wrote them exactly, added the $K_1$ formula and the $p=\tfrac12$ odd-degree symmetry comment, and patched the attached generator to return exact rationals.
* done -- rewrote the normalisation comment to say that rescaling preserves zeros and that the Askey normalisation fixes the row names; checked the linked Askey table slug from the live document.
* done -- removed the uncited `RequestedNumberdbData11` link after checking no `CITE` used it.
* done -- kept the Delsarte keyword and added the Hamming-scheme linear-programming-bound sentence after checking the live Hamming table and the stored scaling relation.
* done -- added and ran a Sage program for one more zero; it printed `1/2` for the row $p=\tfrac14$, $N=5$, $n=2$, $k=1$.
* done -- rewrote the completeness note to state the Hamming cases through degree 4 and the reflected roots explicitly instead of giving only the entry-limit budget.
* declined -- left the $k$ parameter comment because it is correct, local to the parameter, and the critique only noted the repetition.
* declined -- left the entry order because it matches the companion table's increasing-$p$ order and keeps reflected probabilities in a predictable order.
