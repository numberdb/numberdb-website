done -- added the `polynomial` tag after checking the live tags and the current `polynomial` tag; kept `analysis` because dropping it was a separate judgement and the tag still groups numerical-method tables.
done -- added the Chebyshev $U_n$ equivalence to `formula-second-difference`; checked the T99 slug with `numberdb.search_text` and verified every stored second-difference entry for $2\leq n\leq12$ against T99 in Sage.
done -- replaced the `family` constraint's pointer to the comments with a sourced classical-test-matrix constraint; checked the live field was still the old text and rendered the parameter line with resolved citations.
done -- added `CITE{WikiRedheffer}` to `formula-redheffer`; checked live text search still finds Mertens constants rather than a Mertens-function table, and the post-edit audit is clean.
left for a person -- T327 and T329 still render as private drafts to anonymous readers while T237 is public; draft cross-links are audit-clean and the publish-together check belongs at review time.
declined -- the definition is a register difference, not a mathematical ambiguity, and rewriting it would cost more than it gains.
done -- added `Kac-Murdock-Szegő matrix` as a keyword after checking the live keywords had only the unaccented spelling.
done -- marked the Sage snippet as `# Hilbert matrix, n = 3.` and rendered the stored post-edit program preview.
declined -- the $n=2$ row collisions are real equal characteristic polynomials, so there is no table fault to repair.
