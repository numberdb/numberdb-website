1. done -- deleted `Size exception`; checked the live API document still had it, rendered preview showed `(Unknown key)`, and the audit had no size finding.
2. done -- added `Programs.program-sage`; checked `KnotInfo.K3_1.kauffman_polynomial()` under Sage and ran the stored snippet through `agents/sage.sh`, which returned `m=-5` and the trefoil entry.
3. done -- rewrote the Definition to state the $a^{-m}$ shift with $m$ the lowest exponent of $a$ and to cite Rolfsen; checked the live Definition first, then shortened it after audit flagged the first rewrite as too long.
4. done -- appended the chirality converse sentence; checked all 479 live entries against the generator and found the only duplicate number/comment groups were the mirror pairs for $9_{42}$ and $10_{71}$.
5. done -- added the missing curl rule to `comment-convention`; checked KnotInfo's convention page gives the $a$ and $a^{-1}$ curl rules.
6. done -- added `$0_1$, the unknot; $m=0$` to the unknot row; checked the live row had only `number` and `equals`, while the generator showed it was the only entry without a comment.
