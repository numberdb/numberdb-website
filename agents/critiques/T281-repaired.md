audit part. declined -- kept `part` as the three parts of one triple; the live audit after the repair is clean.
audit tree. done -- changed the displayed `tree` parameter to an equivalence-class parameter; checked Montanus for the Hall-tree/equivalence-class correspondence.
1. done -- changed both Formula entries from `{name, text}` records to plain strings; checked the live page no longer contains the Python dictionary representation.
2. done -- added comments defining the Belyi map, Hall dessin and Hall tree; checked the statements against Montanus and rendered the comments.
3. done -- changed the parameter title/display/constraint from tree labels to equivalence classes, with `Birch` displayed as Birch symmetric; checked Montanus gives one class for $M\leq4$ and four for $M=5$.
4. done -- changed the displayed $M$ values from `$M=1$` through `$M=5$` to `1` through `5`; checked the live page no longer contains `M=1`.
5. done -- added Davenport's bound and narrowed the completeness note to every class for $M\leq4$ and only Birch's symmetric class among four for $M=5$; checked the source and exact degrees in Sage.
6. done -- rewrote the equivalence comment with two triples and replaced "printed here" with CITE{Montanus2006}; checked the source's equivalence formula.
7. done -- corrected the T279 and T280 similar-table glosses; checked the rendered page.
8. done -- changed the definition to use half-degree consistently and to avoid the old tree-label wording; checked the rendered definition and parameter list.
