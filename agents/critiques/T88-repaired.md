done -- added 23 entries with $23\in S$ from the complete `curves_maxNS_1000.txt` source, scoped by `complete-note` to subsets of the first eight primes together with 23 whose product is at most 1000; checked the live table first, recomputed $\mathbb{Q}$ counts as curve-list lengths and $\overline{\mathbb{Q}}$ counts as distinct $j=1728c_4^3/(c_4^3-c_6^2)$ values under `agents/sage.sh`, matched 90 existing rows and the published rational-count file, and cross-checked $S=\{2,3,23\}$ against the newer complete j-invariant file.
