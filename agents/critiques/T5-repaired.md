1. *done* -- fixed the definition to quantify over a fixed field, moved the field-dependence caveat into a new comment, and shortened `rigour details`; checked the live API/page still had the old placement and checked the current bound against arXiv 2608.16884.
2. *left for a person* -- the stale dagger target is `numberdb_app/templates/help.html`, not T5; the site bug was already recorded in `docs/agent-environment.md`.
3. *done* -- moving the field sentence out of `rigour details` leaves the ignorance-not-error sentence visible in the note; the separate renderer-fold bug remains recorded in `docs/agent-environment.md`.
4. *left for a person* -- the singleton-tag audit finding is still true, but fixing it means adding `algorithms` to other tables or making a corpus tag decision, not deleting T5's tags.
5. *done* -- added a Coppersmith-Winograd history sentence and `Coppersmith-Winograd` and `laser method` keywords; checked the 1990 exponent against the Coppersmith-Winograd paper and the current 2026 bound against arXiv 2608.16884.
6. *done* -- rewrote `complete-note` as a sentence about the one exponent and the interval between the best proved bounds; checked T5 is still a one-entry, no-parameter table.
(a) *declined* -- did not add T6 as a similar table because the relation is a website convention for wide intervals rather than a mathematical relation between the quantities.
(b) *declined* -- left `sources` naming the upper-bound paper only; the lower endpoint is the trivial lower bound already stated in the comments and rigour note.
(c) *declined* -- left the published title alone because the issue is cosmetic and search already reaches the table by the standard wording.
