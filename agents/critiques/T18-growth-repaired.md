done -- made $|\alpha|$ findable as `alpha_abs`; checked the live lookup had no positive-$\alpha$ match, checked Broadhurst stores signed $\alpha<0$, and checked the positive digits against OEIS A006891 under `agents/sage.sh`.
done -- defined $b$, $c$, $d$ and $\kappa$ through the shortened Definition, Formulas and the sign-convention comment; checked Broadhurst's source before writing the equations and singularity-order statement.
done -- added `complete: yes` and a `complete-note`; checked Broadhurst's source gives exactly the six constants for the period-doubling fixed point, with $|\alpha|$ added only as a convention for the second Feigenbaum constant.
done -- added `Size exception`; checked the live audit's size finding and Broadhurst's statement that the 1018 decimal places took three days at $N=700$ and were checked at $N=720$.
done -- expanded `rigour details` with the 1018/868 digit split and the provenance of $|\alpha|$; checked the stored digit counts under `agents/sage.sh`.
done -- filled `Similar tables` with T168, T169 and T171; checked those live documents for their titles and stated relations to T18 before writing the back-links.
done -- cited `Fei`, so the Feigenbaum 1976 reference is no longer uncited.
declined -- did not add Feigenbaum constants for other critical orders $z$; checked the report's reason against the live table shape, and adding $z$ would change the published parameter identity and belongs in a separate table.
declined -- did not add other universality classes; the report is right that they are different renormalisation fixed points and belong in separate tables linked by relation, not as rows here.
declined -- did not recompute or extend the 1018/868-digit values; the report's own computation made that a rigour opportunity for a future generator, not a repair to this table's range.
