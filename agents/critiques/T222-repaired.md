1. *done* -- regenerated every entry comment with Sage `latex()` in the variable $x$ instead of Sage polynomial strings; checked the live API still had raw `t^10`/`*`, printed representative replacement comments, and verified the stored draft at 345/345 entries.
2. *done* -- replaced "largest real pole" with the smallest-positive-pole statement; checked in Sage that the smallest positive pole is $1/\tau$ for all 345 entries and that all 76 ideal-vertex rows make the old wording false.
3. *done* -- changed "reflection length" to word length with respect to the three reflections and moved the growth-series definition to Formulas; checked the generator's breadth-first Tits-representation controls against the growth-series coefficients.
4. *done* -- added the scoped Salem/Pisot comment, the Floyd reference, golden-ratio links, and the p-bonacci row names; checked all 269 finite-label rows are Salem-shaped, all 76 ideal-vertex rows are Pisot-shaped, the two golden-ratio rows, the plastic row, and the $p$-bonacci polynomials for $p=2,\ldots,12$.
5. *done* -- added a table comment saying distinct triangles can share $\tau$ without sharing $W(t)$, with the two examples from the critique; checked 56 shared values across 130 rows by minimal polynomial.
6. *done* -- replaced the undefined general Steinberg formula with the triangle-specialized formula used by the generator; checked it against the generator's exact rational expression and removed the build narration.
