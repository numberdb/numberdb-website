1. done -- Changed `Wiki-quartic` to Wikipedia's Algebraic number field article; checked the live draft still pointed at the missing Quartic field page, that URL returned 404, and the replacement returned 200.
2. done -- Added the `transcendental` tag; checked live T131 and T158 both carry it and `/api/tag?url=transcendental` lists the existing tag.
3. done -- Added T160 to `Similar tables`; checked T160's degree-four rows are $725$, $-275$ and $117$, the slug resolves, and T364's first entries for signatures $(4,0)$, $(2,1)$ and $(0,2)$ have those discriminants.
4. done -- Removed the uncited `Request15` link; checked the live document cited no `Request15` marker and GitHub issue 15 is an empty request titled "Regulators of number fields".
5. done -- Rewrote `complete-note` as a total with a signature breakdown; checked the live entries still number 456, with 20 signatures $(4,0)$, 180 signatures $(2,1)$ and 256 signatures $(0,2)$.
6. done -- Renamed the coefficient vector in `comment-index` to $(c_3,c_2,c_1,c_0)$ and rewrote `comment-entries` as "$f(a)=0$"; checked the live comments still used $(a_3,a_2,a_1,a_0)$ and the entry comments use $a$ for the generator.
