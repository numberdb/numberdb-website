1. *done* -- added Similar tables entries for T14, T4, T386, T387, T385 and T31 after checking the live neighbouring documents and search-returned slugs.
2. *done* -- removed `both signs` from Data properties after the live page still rendered it as `Unknown key` and the stored entries checked as keys `1` through `1000`.
3. *done* -- changed the Dirichlet series to start at $m=1$ and made the convergence condition a sentence, using $m$ so it does not collide with the table index.
4. *done* -- changed the value header to $\gamma_n$ and defined $\rho_n=\beta_n+i\gamma_n$ in the definition after checking T3 is type `R` and T387 uses $\rho_n$ for complex zeros.
5. *done* -- narrowed the parameter constraint to $n\geq1$, removed the non-rendered negative-index parameter comment, and added a complete note after checking all 1000 entries are positive, increasing, and below 1420.
6. *done* -- rewrote the definition to name $\zeta(s)$, the positive ordinates, the ordering, and the indexing after checking the live table held only positive ordinates.
7. *done* -- added the phrase "Riemann hypothesis" to the comments and Keywords, stated as the hypothesis rather than as a time-dependent claim about computed zeros.
8. *done* -- added a Python/mpmath Programs snippet that prints the ordinate, and checked it under `agents/sage.sh` on entries 1 and 1001.
9. *done* -- replaced the backward-pointing "Its" with "The trivial zeros of $\zeta(s)$" while rewriting comment (2).
10. *done* -- replaced the "whereby" clause with a sentence explaining conjugate zeros and the positive-ordinate convention.
11. *done* -- removed the database-comparison sentence from rigour details, leaving the checked arb enclosure statement.
12. *declined* -- T3 has nothing to change for T385 crowding its first-zero search result; the overlap comes from a theorem, and the skill reserves `repeats` for transcribed computations.
13. *done* -- checked the rendered page after the edit; the old `Unknown key` line is gone, the header is $\gamma_n$, and the new sections render.
