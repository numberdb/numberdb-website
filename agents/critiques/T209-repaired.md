1. done: changed the elementary-row comment to say such rows are omitted; checked the live API/rendered draft and Sage verified the nine elementary $(a,b)$ pairs are omitted while all 282 non-elementary grid entries are present.
2. done: added formulas and DLMF links for the parabolic cylinder and Whittaker functions, and changed the comment to point to those formulas; checked DLMF 12.7.14 and 13.14.3, then verified both identities numerically in the Sage wrapper.
3. left for a person: there is still no public complementary-error-function table to link; public API checks for T197, T204, erfc, and complementary error function did not produce a visible target.
4. left for a person: the Kummer $M$ sibling still has no live table number; an authenticated API probe of T197 through T220 and likely Kummer slugs found no such table.
5. done: defined $(a)_s$ as the Pochhammer symbol and narrowed the Bessel check to the twelve rows in this table with $b=2a$; Sage checked all twelve rows against the Bessel relation.
6. declined: the Ramanujan similar-table gloss is broad but not false, and the critique itself only noted a weaker stylistic alternative.
7. declined: the repeated values are genuine rows of the family rather than duplicate entries; the fresh live document has fifteen repeated value groups, matching the critique's count.
