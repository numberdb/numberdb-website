1. done -- stored rows $1$ through $4$ as `1`, `2`, `6` and `18`; checked the live table still had zero-width intervals, brute-forced $R(n,n)$ for $n\leq3$ in Sage, and checked $R(4,4)=18$ in DS1's Greenwood-Gleason note.
2. done -- rewrote the definition with a separate integer $N$ and a gloss of monochromatic clique; checked the rendered definition after the edit.
3. done -- added reciprocal `Similar tables` links to T151 and T156 using the slugs and relation from their live API documents; did not add the optional T5 link because the critique marked it as a judgement rather than a fault.
4. done -- replaced the $O(1)$ exponent by a constant-$C$ statement scoped to sufficiently large $n$; checked Conlon's Annals abstract and DS1's asymptotics section before changing it.
5. done -- removed the duplicate $5\leq n\leq10$ coverage sentence, made `rigour details` say the entries were not computed, and changed `sources` to DS1; checked the endpoints against DS1 revision #18.
6. done -- added the current asymptotic upper formula from Gupta-Ndiaye-Norin-Wei and a current-bounds comment; checked DS1 2.4(8) and arXiv:2407.19026, which made the critique's CGMS-only wording stale.
7. done -- added comments to the six interval rows naming the lower-bound construction and Angeltveit-McKay upper computation; checked the endpoints in DS1 Tables Ia/Ib and checked the row attributions against DS1's notes and reference list.
8. done -- added the parameter title `clique size`; checked the rendered parameter line.
9. done -- removed both singleton tags, `extremal combinatorics` and `Ramsey theory`; the report's proposal to keep `Ramsey theory` was declined because the audit still refused it and no second live Ramsey table exists.
10. declined -- the OEIS link caption is defensible: direct OEIS access still returned 403 here, but the indexed OEIS text says A059442 is the main array of Ramsey numbers $R(n,k)$.
11. declined -- the Erdős-Szekeres DOI landing on a reprint is a bibliographic wrinkle rather than a table error, and changing it would need a citation policy decision.
12. left for a person -- the stale `[43,48]` example is on `/help#search-precision`, not in T6.
