left for a person -- the live preview still renders `Size exception` with `(Unknown key)`, but that is the renderer's property-name map rather than a table field.
done -- added the CTblLib `F3+` warning to `comment-labels`; checked CTblLib 1.3.11 under Sage, where `Fi24` resolves to `F3+.2` while the table uses `F3+`.
done -- replaced `Size exception` with the measured count; checked 1370 entries, 255 within-group repeated-degree rows, and 1115 rows after dropping those repeats.
declined -- the proposed definition link now makes the live audit fail with `Definition links to another table`; the existing links in Comments and Formulas remain.
done -- added the missing sporadic group names to `Keywords`; checked the live draft still lacked those names before editing.
done -- shortened the `chi` constraint to the actual constraint and omission reason; checked the original field still repeated the definition.
done -- replaced the repeated-degree comment; checked the entries still verify against the generator after the prose edit.
done -- rewrote the moonshine formula to name `$\chi_2(1)$` and link `Q-expansion_of_the_j-invariant#1`; checked that row `1` is 196884.
done -- changed the Monster symbol in the formula and the Fi24 notation in the label comment to `\rm`; checked the final readback and audit.
