1. *done* -- Replaced the definition with the falling-factorial sum and removed `comment-variable`; checked all 210 live rows against that sum in Sage.
2. *done* -- Added the Hermite-polynomial Similar tables row and DLMF 18.21.9 link; checked `Hermite_polynomials_H` resolves to the physicists' table and verified the Charlier-to-Hermite limit symbolically in Sage for $0\leq n\leq20$.
3. *done* -- Removed the `mu` keyword after confirming the live draft still had it and kept the existing comment that some sources write the Poisson mean as $\mu$.
4. *done* -- Removed the draft-workflow wording from `rigour details`; checked the edited sentence in the rendered preview.
5. *done* -- Added the range reason to `complete-note`, naming the grid of small rational Poisson means around the OEIS A046716 case $a=1$; checked the live stored $a$ values and the existing OEIS link.
6. *done* -- Moved the orthogonality identity from Comments to Formulas; checked the identity exactly for every live stored $a$ and every $0\leq m,n\leq20$ in Sage.
