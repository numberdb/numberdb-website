left for a person -- The title still reads `Euler's constant e`; I checked the live API and rendered page and left the proposed retitle and naming comment alone because a public-table rename is a person's decision.
done -- Rewrote `rigour details` as reader-facing prose saying the entry is the decimal truncation of `$\exp(1)$`; I checked the live stored decimal with `agents/sage.sh /tmp/t8_check_exp.py`, which put it 0.5517 ulp below `exp(1)` and inside one last-place unit.
done -- Rewrote the definition to name the function `$x \mapsto a^x$`; I checked the live definition still had the old wording and that the derivative condition reduces to `\log a = 1`, hence `a=e`.
declined -- Left `Similar tables` empty because the critique says that is right, and the live document still has no family relation needing a link.
declined -- Left the 1024-bit `Programs` line alone because it computes the entry, while the rewritten rigour note now names the separate 4000-bit recomputation used to check it.
declined -- Left the tags without adding prose because they are true but do not need a comment apart from the title/naming decision left for a person.
declined -- Left the unbroken 301-digit display alone because it is a site-wide rendering issue, not a table-document repair.
