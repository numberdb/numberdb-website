done -- Replaced all 50 entry comments with exact relations to $P_n$, $Q_n$ or $E$ and the linked root; before changing them I checked in Sage that every stored polynomial divides the named family polynomial, with quotients $x-1$ in 12 rows, $1$ in 26 rows and $x^2-1$ in 12 rows.
done -- Kept the keywords and made them true on the page by naming the plastic ratio in row 1, the supergolden ratio in row 4 and the Mahler-measure fact in a table comment; I checked T286's rows 1 and 4, the two stored polynomials, and in Sage that every row has exactly one root outside the unit circle.
done -- Rewrote `comment-classification` so the irreducible factors are explicitly factors of $P_n$, $Q_n$ or $E$ in Formula CITE{formula-families}; I checked the cited formula key was present and the changed section rendered in preview.
done -- Deleted `comment-roots`, leaving the actual row links and the Similar tables relation to T286; after the write I checked the live API document, rendered preview snippets and `GET /api/table/T300/audit`, which returned clean.
