* already fixed -- the live audit now returns clean on the unchanged draft; the definition and `formula-cotes` already say that $C_{n,j}=w_{n,j}/n$, so the normalisations determine one another.
* left for a person -- the prose still names the storage keys in `comment-step`, `complete-note` and the parameter constraint; I prepared and previewed a symbol-based rewrite, but the combined write was refused for changing parameter order and the prompt required stopping.
* left for a person -- regrouping the entries as $n$, normalisation, then $j$ was prepared and checked in Sage to preserve every stored value and moment identity, but `POST /api/table/T302` refused it with `This edit changes the table parameters` from `['n', 'j', 'normalisation']` to `['n', 'normalisation', 'j']`.
* left for a person -- Sage verified that $n=8$ is the first negative rule, that $n=9$ is positive, and that $n=10,11,12$ have negative weights; the corrected sentence was previewed, but no table edit was written after the parameter-order refusal.
* left for a person -- the Gauss--Legendre relation still uses $n$ in the other table's sense; I prepared the wording "the interpolatory rule with free nodes", but no table edit was written after the parameter-order refusal.
* left for a person -- the title still does not say closed; I prepared "Weights of the closed Newton--Cotes rules" with the old title moved into Keywords, but no table edit was written after the parameter-order refusal.
* left for a person -- I prepared removal of the redundant ordering and neighbours comments, shorter common-denominator entry comments for $n\geq5$, and a rigour note saying "moment equations" rather than "Lagrange moment equations"; Sage checked the moment equations, and the range-stop reason remains for a person because I could not verify the builder's reason.
