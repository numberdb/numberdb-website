1. done -- Replaced the live Sage program's `CBF.bernoulli` call with `sage.arith.misc.bernoulli`; checked the old snippet failed under `agents/sage.sh`, the replacement matched the stored $A$ and $\log A$ rows for $k=0,1,19$ exactly, and the generator verified 42/42 after the write.
2. done -- Rewrote `comment-product` to state the product-asymptotic role and remove the undefined normalising-polynomial and limit sentence; checked Wikipedia's generalization section and Dowker's arXiv abstract before writing, then previewed the rendered comment.
3. done -- Shortened the Definition and moved the Bernoulli reference to `comment-notation` and `Similar tables`, because `audit_table` refuses `HREF` in Definitions; checked `Bernoulli_numbers` resolves to T13 and the local partial audit reported nothing.
4. done -- Replaced the Factorial similar-table relation with the explicit Stirling-formula role of $A_0=\sqrt{2\pi}$; checked the `Factorial` slug resolves and previewed the rendered relation.
5. done -- Changed `formula-barnes-g` to link `HREF{Values_of_the_Barnes_G-function_at_rational_numbers#1/2}[$G(1/2)$]`; checked T93 has the `1/2` entry and previewed the direct entry link.
6. left for a person -- The `complete-note` still gives the range $0\leq k\leq20$ without a reason, because I found no source or build decision that justifies 20.
7. done -- Changed `comment-classical` so it states $A_0=\sqrt{2\pi}$ rather than calling the row a number; left the parameter-title and OEIS-link notes alone because they are minor wording/selection decisions beyond the needed repair.
