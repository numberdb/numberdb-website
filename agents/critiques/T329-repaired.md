1. done -- Replaced `comment-signs` with the Fiedler/Ris/Wilkinson counts; checked in Sage from exact characteristic polynomials for every stored order that Fiedler has $n-1$, Ris has $\lfloor n/2\rfloor$, Wilkinson has one, and the stored signs match.
2. done -- Added the exact-rational case to `rigour details`; checked in Sage that the characteristic polynomials have 46 rational roots and that those are exactly the 46 stored exact entries.
3. done -- Rewrote the Clement omission sentence to name the default and symmetric Clement forms; checked T327's Clement definition and verified the integer spectrum $\pm(n-1),\pm(n-3),\ldots$ in Sage for $2\leq n\leq12$.
4. done -- Replaced the `family` constraint's pointer to the comments with the named source collections; checked the live rendered parameter list before changing it.
5. done -- Named MATLAB's `gallery` and the Test Matrix Toolbox in `comment-symmetric-only`; checked those citation keys are declared in the live table.
6. done -- Changed the reader-facing Kac-Murdock-Szegő parameter value and comment spelling, leaving the plain `Kac-Murdock-Szego matrix` keyword for search; checked T327 uses the accented display form.
7. declined -- The KMS definition $A_{ij}=2^{-|i-j|}$ is already unambiguous, and adding the default-parameter explanation is not needed to repair a fault.
8. done -- Replaced the vague Gauss-Kuzmin-Wirsing similar-table relation with T237, characteristic polynomials of connected graphs; checked T237's live title and definition first.
9. done -- Expanded `complete-note` to explain the $2\leq n\leq12$ range as the nontrivial small orders and the 847-entry size; checked the live document has exactly 847 entries.
10. declined -- `HREF{T327}` still points to a sibling draft, which is acceptable while T329 is also a draft; the post-repair API audit is clean.
11. left for a person -- The tag change should be decided together with T327, as the critique says; T329's tags were left unchanged and the post-repair API audit is clean.
