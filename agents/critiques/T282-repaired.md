1. done -- replaced the family comment with Boyd's conjectural $m_k=r_kL'(E_k,0)$ relation and narrowed the proved statement to $k=1$; checked the live table still had the vague text, Rogers-Zudilin's theorem, and Sage's conductor-15 $L(E,2)$ value against the stored $m_1$.
2. done -- added `equals: HREF{Entropy_constants_of_lattice_models#spanning-tree,square,entropy}` to $k=4$ and tightened the similar-table gloss; checked T153's entry and Sage agreement with the stored $m_4$.
3. done -- replaced the $k=1$ entry comment with $m_1=\frac{15}{4\pi^2}L(E_{15},2)=L'(E_{15},0)$; checked the paper statement and Sage numerical agreement for the conductor-15 curve.
4. done -- linked Catalan's constant to `HREF{Values_of_Dirichlet_L-functions_at_positive_integers#4,3,2}`; checked T145's parameter order and that the public anchor renders.
5. done -- split the rigour note into two paragraphs and removed the build-size sentence; checked the live text, then the API audit and rendered preview after the edit.
6. done -- changed the parameter title from "parameter" to "constant term"; checked the live Parameters section still had the uninformative title before editing.
