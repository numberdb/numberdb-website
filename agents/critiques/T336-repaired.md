1. *done* -- added the Hirzebruch signature theorem as `formula-signature`; checked the cited L-genus section states the formula.
2. *done* -- reordered `rigour details` so the independent checks are visible before the fold; checked the edited preview and reran the generator verification.
3. *done* -- merged `LGenusWiki` into the sectioned `GenusWiki` link and retargeted its citations; checked that `L-genus` is a redirect to that article section.
4. *done* -- added a Sage `Programs` block computing $L_7$; ran the exact snippet with `agents/sage.sh` and previewed it.
5. *done* -- removed `formula-zero` and rewrote `comment-zero`; checked the live document still had the duplicated statement before changing it.
6. *done* -- replaced the attached `generate.py` without the deployment-specific publish paragraph; fetched the public attachment afterwards to check it was gone.
7. *done* -- changed the Todd and Chern-character links to published slugs and replaced the proposal-language relation; checked the slugs return 200 and the audit accepts the HREFs.
8. *done* -- defined and linked $e_j$ in `formula-component`; checked the rendered preview resolves the link.
9. *done* -- made `formula-first` say the first four components are printed in the source; checked the cited L-genus section prints $L_1$ through $L_4$.
10. *done* -- named the bundle $E$ in the definition and removed the repeated "variables are Pontryagin classes" sentence; checked the Pontryagin convention in the cited article.
11. *done* -- replaced the range comment with the coefficient formula for $p_n$; checked it in Sage for $1\leq n\leq7$ and reran the generator's $p_7$ check.
12. *done* -- reworded the rigour note to avoid the ambiguous "even-dimensional complex projective space"; checked the projective-space signature test by rerunning the generator.
13. *declined* -- the definition, parameter title and characteristic-series formula already pin down the grading, so adding another sentence would not pay for itself.
14. *declined* -- this was a confirmation rather than a requested repair; I checked the live table still has the six rows ordered $n=1,\dots,6$.
