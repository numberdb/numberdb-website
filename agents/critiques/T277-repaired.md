done -- rewrote the Definition to state that the table stores the 1RSB prediction for the satisfiability threshold, and moved the random-clause convention into `comment-conventions`; checked MMZ's random $K$-SAT description and the final audit.
done -- changed the T273 relation to say that the pure literal rule threshold is clause density $2c_{k,2}$; checked live T273's `comment-peeling` and doubled the $c_{k,2}$ rows in Sage.
done -- restored Mézard and Zdeborová in the prose, the $k=3$ entry comment, the completeness note, and the bib entries; checked the MMZ and KMRTSZ DOI metadata.
done -- removed the $k=4,5,6,7$ row comments; checked that `rigour details` still states the MMZ source and the KMRTSZ rounded-value comparison.
done -- introduced $k_0$ before using it and rewrote the $k=2$ note with a Chvátal-Reed citation; checked the DSS abstract and the Chvátal-Reed DOI metadata.
done -- added `random 3-SAT threshold` to `Keywords`; the draft itself cannot answer search, but the live document had no `3-SAT` search phrase before the edit.
