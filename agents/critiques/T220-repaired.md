1. *done* -- Closed the Tits group's family value as `${}^2F_4(2)'$`; checked the live API still had the unclosed `${}^2F_4(2)'`, and the rendered `/T220` page still answered 404 for this private draft even with the bearer key.
2. *done* -- Added reciprocal comments for $A_1(7)$/$A_2(2)$ and same-order, non-isomorphic comments for $A_2(4)$/$A_3(2)$, plus a table comment for the non-isomorphic equal-order exceptions; checked the live duplicate-order set, GAP isomorphism tests, and Garge's arXiv abstract before writing it.
3. *done* -- Added order formulas for every family, with $B_n$ and $C_n$ combined, and pointed the notation and provenance text at those formulas; checked the formula implementation against all 893 live rows.
4. *done* -- Replaced alternating-group $A_5$, $A_6$, $A_8$ and $|A_n|$ prose with $\operatorname{Alt}_5$, $\operatorname{Alt}_6$, $\operatorname{Alt}_8$ and $|\operatorname{Alt}_n|$; checked the small GAP isomorphisms first.
5. *done* -- Regenerated entry comments with braced $B_{10}$/$C_{10}$-style subscripts and $E_6$, $E_7$, $E_8$, $F_4$, $G_2$ labels; checked selected live rows and generator verification after the write.
6. *done* -- Gave the `family` parameter a plain display and the title "Lie type", and rewrote the $n$ constraints to state the rank bounds and the $C_2/B_2$, $D_3/A_3$ omissions; checked the renderer's parameter-display rule and the generator's rank ranges.
7. *done* -- Replaced the deferring definition with a 270-character definition naming the finite simple Lie-type groups whose orders are stored; checked the classification-page notation and the API-backed audit-length check.
8. *done* -- Rewrote `comment-notation` to list the isomorphic duplicate rows and `comment-exclusions` to identify the represented derived subgroups; checked the classification page's small-group and non-simple-case lists and GAP/order checks where available.
9. *done* -- Rewrote the complete note so groups, not families, have orders, and removed "listed"; checked the live range count stayed 893 and no stored number changed.
