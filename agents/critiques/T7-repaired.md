1. done -- changed the definition to the unit disk and the geometric formula to circumference; checked the live API/page still had the old unit-circle/perimeter wording before editing, and read back the rendered page after.
2. done -- replaced the rigour details with a provenance sentence naming `RealBallField(1024).pi()`; checked in Sage that the stored value is about 0.2754 last-place units from pi recomputed at 4000 bits and that the 1024-bit ball lies inside the stored last-place interval.
3. done -- replaced the prose-only formula entry with $C=2\pi$ for the unit circle's circumference and added $\pi=4\arctan 1$ and $\sum_{n\geq 1} n^{-2}=\pi^2/6$; checked the arctangent and Basel identities in Sage ball arithmetic.
4. declined -- the repeated phrase about scaled values serves different jobs in Similar tables and the completeness note, so changing it would be taste rather than repair.
5. left for a person -- whether `trigonometry` should be replaced by a corpus-wide geometry tag affects several tables, not just T7.
6. declined -- T29 already links back at the entry level with `equals`, which is stronger than a page-level Similar tables back-link.
7. declined -- a past no-message revision cannot be repaired through a table-content edit; this repair's API revision was sent with the message `repair pi definition and provenance`.
