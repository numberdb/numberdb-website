left for a person -- the link to T222 still points at a draft; T223 is also a draft, and deciding/publishing the pair is review work I may not do.
done -- finding 1: corrected the Kellerhals reference to Kellerhals-Kolpakov, replaced the dead DOI with `10.4153/CJM-2012-062-3`, and removed `arxiv`; checked the old DOI 404, the corrected DOI redirect/Crossref record, and that arXiv:0912.1384 is an unrelated nanotube paper.
done -- finding 2: corrected the live and generator Coxeter symbols to `[5,3^{1,1}]`, `[(3,3,3,4)]`, `[(3,3,3,5)]`, `[(3,4,3,4)]`, and `[(3,5,3,5)]`; checked the Coxeter notation source and preview rendering.
done -- finding 3: replaced the placeholder `Programs` block with a Sage `growth_rate(M)` snippet; checked it with `agents/sage.sh` on `[3,5,3]`, `[5,3^{1,1}]`, `[(3,5,3,5)]`, and the triangle control `[7,3]`.
done -- finding 4: added the Salem-number keyword and comment, sorted entries by growth rate, and marked `[3,5,3]` as the unique minimum; checked Crossref's Kellerhals-Kolpakov abstract and Sage verified all nine irreducible reciprocal polynomials and the stored minimum.
done -- finding 5: removed `comment-range` and moved the range reason into `complete-note`; checked the live duplicate was still present and the nine compact labels against the Coxeter notation list.
done -- finding 6: defined $W_T$ and $W_T(t)$ in `formula-steinberg`; checked the formula by rerunning the generator verification and the replacement Sage snippet.
done -- finding 7: shortened the Definition and rewrote `comment-generators` so the orientation-preserving subgroup is a different number, not a convention; checked the final definition is below the audit threshold.
done -- finding 8: rewrote the T222 similar-table relation to describe dimension-$2$ simplex groups, the hyperbolic Coxeter triangles; checked T222's live `[7,3]` key while checking the Lehmer link.
left for a person -- noticed one-table-or-two: I did not merge T222 and T223 because both are drafts and that is the owner's structural decision.
declined -- noticed tags: I left the tags alone because the current tags all reach other tables, and changing browse categories was not needed to repair a ranked defect.
done -- noticed rigour details: removed the workflow phrase "Before the draft was filled" and narrowed the check sentence to what I verified.
declined -- noticed entry-comment fragments: I corrected the affected symbols and added the minimum fact, but left the repeated row labels because they are not false and removing them would be noisy churn.
declined -- noticed Viswanath similar table: I left the relation alone because it is not false, just less central than the Coxeter-family repairs.
