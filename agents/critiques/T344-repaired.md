1. *done* -- changed `complete` from `false` to `no`; checked the live API still had `false` and the kept generator `table.yaml` already had `no`.
2. *done* -- replaced "specialises" in the division-polynomial sibling relation; checked the live sibling definition uses reduced global minimal Weierstrass models, not generally short models.
3. *done* -- changed the ranks-table relation to say it records the highest known rank for each rational torsion subgroup; checked the live sibling title and definition.
4. *done* -- replaced the root sentence with a specialization to nonsingular fields of characteristic not dividing $2n$; checked the live definition already fixes the short model and verified the stored $\psi_n$ convention in Sage.
5. *done* -- added a Sage `Programs` snippet for $n=8$; checked the exact displayed snippet through Sage's preparser under `agents/sage.sh`.
6. *done* -- added Sage's two division-polynomial conventions and a Sage reference link; checked all seven stored values against Sage and checked the new link returned 200.
7. *done* -- moved the coordinate-ring qualifier into the even recurrence; read the changed formula back as prose before sending it.
8. *declined* -- kept the opening variables sentence because it names the literal variables in the stored polynomials, and the critique itself says it should survive the root-sentence repair.
