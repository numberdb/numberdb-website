1. done -- padded the 28 nonzero `KNOTINFO_CS` strings to nine decimal places, stopped discarding a supported scaled digit, used `+/-` intervals for the two scaled $10_{103}$ rows whose balls were findable but decimal shorthand was not, and checked the generator against SnapPy before writing; `verify(sample=None)` reported 932/932 matched, with only the four $10_{20}$ rows still not findable from the source precision.
2. done -- changed `Formulas` from mappings to plain formula strings and checked the rendered page no longer contains dictionary reprs.
3. done -- changed the title to name the hyperbolic prime knots with at most ten crossings and confirmed the audit is clean.
4. done -- rewrote the Definition to cite Kirk-Klassen and Neumann and state the modulo-$1/2$ target; the representative and source convention now live in keyed comments, and the audit is clean.
5. done -- added a SnapPy `Programs` snippet with the Rolfsen-Perko naming warning from the sibling table.
6. done -- split the single unkeyed comment into keyed comments for the excluded knots, numbering, mirror rows, normalisation, and representative convention.
7. done -- explained the missing trefoil in the $n$ constraint and sorted the listed torus knots by crossing number.
8. done -- changed the mirror-row explanation so distinct knots justify storing both rows, while sign reversal is stated as the formula it is.
9. done -- replaced the tautological normalisation formula with the complex-volume relation and checked it in Sage/SnapPy on $5_2$.
10. done -- changed the representative interval to $[-1/4,1/4)$.
11. done -- added the amphichiral-zero argument from amphichirality plus the mirror sign formula.
12. done -- changed the `normalisation` parameter title and constraint so the rendered line says which multiple is listed.
13. declined -- the critique checked that hyphen/en-dash spelling does not affect search, and changing the established title spelling would not improve the table.
14. left for a person -- a knot/topology tag would affect this table and its siblings and needs a corpus-level tag decision.
15. declined -- duplicate entry comments on both normalisations are intentional enough to avoid an apparent omission, and the size cost is acceptable.
