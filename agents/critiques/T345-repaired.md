1. done -- removed the four "Requested in ..." table links and the generator's false "#126, #127, #128" answer claim after checking the live table and GitHub issue titles; only #10 is about elliptic-curve division-polynomial roots.
2. done -- made the Definition state that the roots of $K_\varphi(x)$ are the $x$-coordinates of the nonzero points of $\ker\varphi$ and removed the separate roots comment; the final definition was shortened until `GET /api/table/T345/audit` was clean.
3. done -- replaced the degree 2 and 3 comment with the linear-polynomial rationale; Sage checked all $N\leq300$ degree 2 and 3 rational-isogeny kernel polynomials are linear rational polynomials.
4. done -- revised `complete-note` to say the range is source conductor $N\leq300$ and that Sage's mini Cremona database is complete in that range; Sage checked 1,172 curves through conductor 300 and exactly the 118 stored $\ell\geq5$ rows.
5. done -- linked the first Formula-section mention of the division-polynomial table; the live rendered page shows the `HREF` resolved.
6. done -- extended Formula (2) with the leading-coefficient explanation for nonintegral monic factors; Sage checked the stored denominators are $1$ or $\ell$ and that the relevant $\psi_\ell$ leading coefficient is $\ell$ on every row.
7. done -- changed the Definition so $E/\mathbb Q$ is the recovered elliptic curve rather than calling a model the object being mapped by an isogeny; the model details remain in Formula (1).
8. done -- rewrote `rigour details` to name the PARI/GP `ellisogeny` check from the stored kernel polynomial and added a cited PARI link; the retained generator was run through `agents/sage.sh` and reported PARI target invariants for every row.
9. done -- changed the attached generator and repository copy to enumerate `source.isogenies_prime_degree()` and filter $\ell\geq5$, instead of using the fixed seven-degree tuple; `agents/sage.sh generators/kernel-polynomials-rational-isogenies-elliptic-curves-q/generate.py` passed with the key.
10. done -- kept the `Velu isogeny` keyword and earned it with a comment saying the kernel polynomial is input to Velu's formulas, with a cited Sage reference; the rendered page shows the citation and the final audit is clean.
