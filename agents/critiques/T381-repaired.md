1. done -- removed the build-machine `agents/sage.sh` publish paragraph from the attached `generate.py`; checked the live file still contained it, then reran the repaired generator under Sage and got 5/5 stored values matched.
2. done -- named and linked `HREF{Elementary_symmetric_polynomials}[the elementary symmetric polynomials]` in `rigour details`; checked T118 is the elementary symmetric polynomial table and the generator's root-product substitution check still passed for all stored degrees.
3. done -- changed the provenance ring in `rigour details` from $\mathbb Z[a_0,\dots,a_5]$ to $\mathbb Z[a_0,\dots,a_n]$; checked the live entries are written only in $a_0,\dots,a_n$ and the generator matched all stored values.
4. done -- added `quadratic discriminant`, `cubic discriminant`, `quartic discriminant` and `quintic discriminant` to `Keywords`; checked the search index weights `Keywords` with the title and live suggestions for cubic/quartic discriminant were ranking field tables before this draft can publish.
5. done -- rewrote `comment-source` to name Wikipedia's discriminant article instead of "The source"; checked the rendered draft shows the citation as `[1]` with no broken citations and `/api/table/T381/audit` is clean.
