1. done -- braced the generated `$a_p$` subscripts in row comments and attached the patched generator; checked the live API/page had 44 broken multi-digit subscripts before the edit and none after it.
2. done -- replaced the table comment's Atkin-Lehner sentence with the $U_p$ eigenvalue relation, removed the 42 $p\parallel N$ row restatements, and narrowed the five $p^2\mid N$ row comments to `$a_p=0$`; checked in Sage that all 42 $p\parallel N$ rows have $a_p=-w_p$ and all five $p^2\mid N$ rows have $a_p=0$.
3. done -- defined $K_f=\mathbb{Q}(a_n:n\geq1)$ in the Definition and moved the degree fact into `comment-characteristic` after audit flagged the first draft as too long; checked Sage's coefficient fields for the 50 rows and checked the LMFDB `dim` field matched the stored field degree for every listed orbit.
