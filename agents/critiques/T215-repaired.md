1. done -- removed all 164 live entry comments, after checking the rendered rows still had them and that the formulas/comments now carry the weight-12 and degree facts.
2. done -- added a Maeda comment and Hida-Maeda/Ghitza-McAndrew references; checked the citation details against the papers' pages and checked in Sage/SymPy that all 164 live polynomials are irreducible over $\mathbb{Q}$, leaving the Galois-group claim as conjectural and explicitly unchecked for these rows.
3. done -- replaced the degree formula with the level-one dimension formula and shortened the zero-space comment; checked every live row degree against it and checked the formula against Sage's `CuspForms(1,k).dimension()` for even $4\leq k<100$.
4. done -- defined $M_j$ and $M_*$ where they first appear, linked $\Delta$, $E_4$ and $E_6$ at their first formula/rigour-detail mentions, and made the similar-table glosses stand alone; checked the rendered prose and `audit_table T215 --links`.
5. done -- removed the range-size sentence from `rigour details` and attached it in `generate.py` instead; checked the rendered rigour details no longer contain the 60.8 KB note and that `manage.py audit_table T215` reports nothing.
