already fixed -- The live table no longer has the 32-row, three-field coverage described in the report: `GET /api/table?id=T293` now has 1171 rows over $D\in\{5,8,12,13,17,21\}$ with conductor norm at most $250$; checked against pinned ecnf-data and found 1171 expected rows, 1171 stored rows, 0 missing and 0 extra.
already fixed -- The claim that $D=5$ and $D=8$ have no positive-rank curves at conductor norm at most $60$ is now reflected in `comment-small-fields`; checked pinned ecnf-data and found minimum positive-rank conductor norms $199$ and $103$ respectively.
already fixed -- The requested conductor-bound increase to $250$ has already been made; checked pinned ecnf-data counts at that bound as $17,63,253,195,295,348$ for $D=5,8,12,13,17,21$, matching the live table exactly.
declined -- I did not extend the conductor-bound to $500$: the live table already has 1171 rows, while the same source check gives 3577 positive-rank rows over the current six fields at bound $500$, exceeding the 1200-entry soft limit.
already fixed -- The request to add at least one more field is already acted on by the live $D=21$ block; checked pinned ecnf-data and found 348 expected $D=21$ rows at bound $250$, all present in the live table.
declined -- I did not add $D=24,28,29,33,\ldots$: at the same conductor-bound, pinned ecnf-data would add 577 rows for $D=24$ alone, 591 for $D=28$, 285 for $D=29$, and 953 for $D=33$, so this needs a person's size/range decision.
already fixed -- The source is still the pinned ecnf-data commit `10b28418e80392032b106ea00e6c5aa109d28e7b`; the live rigour note states that source, and the coverage/value checks read the raw repository at that commit.
already fixed -- The 35-significant-digit convention is still in the live rigour note; checked every stored regulator against the pinned source `reg` field with one-last-place tolerance and found 0 mismatches.
already fixed -- The rank statement has already been rechecked and narrowed: pinned ecnf-data gives only rank $1$ rows for $D\in\{5,8,12,13,17\}$ and, for $D=21$, 340 rank $1$ rows and 8 rank $2$ rows at bound $250$.
already fixed -- The live rigour note still records the preserved checks against source generator heights, rank-$2$ height-pairing determinants, and the BSD quotient; because I made no table edit, I left those recorded checks untouched and the audit endpoint returned clean.
declined -- I made no API edit: the live table already implements the useful part of the report, and the remaining extensions would exceed the soft entry limit before a reviewer chose a new range.
