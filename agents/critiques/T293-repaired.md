already fixed -- The live API document and rendered page no longer have the 32-row, three-field coverage described in the report; the pinned ecnf-data check found 1171 expected rows over $D\in\{5,8,12,13,17,21\}$ at conductor norm at most $250$, exactly matching the live table.
already fixed -- The proposed small-field comment is present; the pinned ecnf-data check found 0 positive-rank rows for $D=5$ and $D=8$ at conductor norm at most $60$, and minimum positive-rank conductor norms $199$ and $103$.
already fixed -- The conductor-bound increase to $250$ has already been made; pinned ecnf-data gives counts $17,63,253,195,295,348$ for $D=5,8,12,13,17,21$, matching the live table by label and stored regulator string.
declined -- I did not extend the conductor bound to $500$: the same pinned-source check gives 3577 positive-rank rows over the current six fields at that bound, which exceeds the 1200-entry soft limit.
already fixed -- The request to add at least one more field is already acted on by the live $D=21$ block; pinned ecnf-data gives 348 expected $D=21$ rows at conductor norm at most $250$, all present.
declined -- I did not add $D=24,28,29,33,\ldots$: at the same conductor bound, pinned ecnf-data would add 577 rows for $D=24$, 591 for $D=28$, 285 for $D=29$, and 953 for $D=33$.
already fixed -- The source is still the pinned ecnf-data commit `10b28418e80392032b106ea00e6c5aa109d28e7b`; the live rigour note states that source, and the source check read the raw repository at that commit.
already fixed -- The 35-significant-digit convention is still in the live rigour note; the source check truncated each pinned `reg` value to the table convention and found 0 stored regulator mismatches.
already fixed -- The rank statement has already been rechecked and narrowed: pinned ecnf-data gives only rank $1$ rows for $D\in\{5,8,12,13,17\}$ and, for $D=21$, 340 rank $1$ rows and 8 rank $2$ rows at conductor norm at most $250$.
already fixed -- The live rigour note still records the preserved checks against source generator heights, rank-$2$ height-pairing determinants, and the BSD quotient; because I made no table edit, I left those recorded checks untouched.
declined -- I made no further size-driven extension: the live table has 1171 entries, below but close to the 1200-entry soft limit, so further extension would need a person to choose a range or approve a size exception.
