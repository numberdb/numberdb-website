*already fixed* -- The live table no longer has 32 rows or a conductor-norm cutoff of $60$; `GET /api/table?id=T293` and the rendered `/T293` page on 2026-09-19 show 1171 rows over $D\in\{5,8,12,13,17,21\}$ with conductor norm at most $250$, and the attached generator verified `1171/1171 matched`.
*already fixed* -- The claim that $D=5$ and $D=8$ contribute nothing below norm $60$ is recorded as a table comment; I checked the pinned ecnf-data source with Sage and found no positive-rank rows at norm at most $60$, with smallest positive-rank conductor norms $199$ and $103$.
*already fixed* -- The conductor-norm bound has been raised to $250$; the pinned source and live table both give 17 rows for $D=5$ and 63 rows for $D=8$.
*declined* -- I did not raise the bound to $500$: over the current six fields the pinned source has 3577 positive-rank rows at that bound, not a small or roughly doubled extension, and it would far exceed the 1200-entry soft limit.
*already fixed* -- The table no longer stops at the five fields in the old note; $D=21$ is included, with 348 live rows checked from the pinned source.
*declined* -- I did not add $D=24,28,29,33,\ldots$: $D=24$ alone would add 577 rows at the current bound and push the table past the soft entry limit, so extending the field range further needs a person to choose a new range or a size exception.
*already fixed* -- The source remains ecnf-data at commit `10b28418e80392032b106ea00e6c5aa109d28e7b`; the checked generator reads that source directly and the live table cites the same commit in rigour details.
*already fixed* -- Values are kept to 35 significant digits; the live generator has `DIGITS = 35`, the rigour note explains the unstable final source digits, and the full generator verify matched every live value.
*already fixed* -- The rank claim was rechecked rather than carried over: the live rigour note says $D\in\{5,8,12,13,17\}$ rows have rank $1$ and $D=21$ includes rank $1$ and rank $2$; parsing the live comments gave 340 rank-1 and 8 rank-2 rows over $D=21$.
*already fixed* -- The generator keeps the existing checks: rank-1 regulators against source heights, rank-2 determinants recomputed in Sage, and the BSD quotient against the ecnf-data analytic order of Sha; running the generator and full verify succeeded, and `/api/table/T293/audit` returned `clean: true`.
