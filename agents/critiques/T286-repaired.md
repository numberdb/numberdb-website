1. done -- fixed the Sage program to sort by the root interval and renamed the interval field; before changing it I ran the old and new programs under `agents/sage.sh` and checked that the new output gives rows 1 through 10 in order.
2. done -- moved the classification into `formula-families` and shortened `comment-classification` to cite that formula; before changing it I checked the live prose still had the duplicated generator description and verified the first 55 generated labels against the row order.
3. done -- sharpened the T222 relation and linked the $P_2$ through $P_{11}$ row comments to the matching Coxeter entries; before changing it I read live T222 and checked the `[inf,3]` through `[inf,12]` rows have the same values and minimal polynomials.
4. done -- replaced the Mahler-measure implication with the Pisot-conjugate reason; before changing it I checked all 50 stored minimal polynomials have constant term $\pm1$ and that all conjugates except the Pisot root lie inside the unit disc.
5. done -- rewrote the row comments as sentences naming roots of $P_n$, $Q_n$ or $E$, and added the smallest-Pisot and supergolden notes; before changing it I parsed the live row comments and checked the generated labels against the stored order.
6. done -- added the Dufresnoy-Pisot smallest-limit-point sentence and linked $\varphi$ to `Golden_ratio#phi`; before changing it I checked the source claim against the live references and the current Wikipedia/MathWorld summaries.
7. done -- removed the build-size note from rigour details and added the monotonicity/rank argument; before changing it I checked in Sage that rows 49 and 50 are $P_{25}$ and $Q_{26}$ and that $P_{26}$ and $Q_{27}$ are already larger than $\theta_{50}$.
8. done -- replaced the completeness note with the exact covered range and the reason for stopping at 50; before changing it I checked $\varphi-\theta_{50}\approx2.6659\cdot10^{-6}$ and that the first 50 generated roots are exactly the table's rows.
